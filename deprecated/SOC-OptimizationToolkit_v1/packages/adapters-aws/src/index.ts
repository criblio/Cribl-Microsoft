// @soc/adapters-aws — AWS source connector (SourceConnector via AWS SDK v3: S3/SQS/Kinesis/CloudWatch/IAM).
// Implements core ports outside the pure core; real implementation lands in Phase 8.
// Shell for now (Phase 0 scaffold).
export const ADAPTER_ID = 'aws' as const;
