// @soc/adapters-fs — ContentRepo/FileSystem/Keystore/LookupSource (Sentinel fetch, EDR blocklist, registry sync, AD/LDAP).
// Implements core ports outside the pure core; real implementation lands in Phase 1-10.
// Shell for now (Phase 0 scaffold).
export const ADAPTER_ID = 'fs' as const;
