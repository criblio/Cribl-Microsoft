// @soc/adapters-identity — IdentityClient: Microsoft Graph (O365/Entra app-registration validation).
// Implements core ports outside the pure core; real implementation lands in Phase 10.
// Shell for now (Phase 0 scaffold).
export const ADAPTER_ID = 'identity' as const;
