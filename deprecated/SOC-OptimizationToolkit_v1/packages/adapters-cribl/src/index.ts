// @soc/adapters-cribl — Cribl Stream control plane (CriblClient): official TS SDK + generated client + vendored overrides.
// Implements core ports outside the pure core; real implementation lands in Phase 2.
// Shell for now (Phase 0 scaffold).
export const ADAPTER_ID = 'cribl' as const;
