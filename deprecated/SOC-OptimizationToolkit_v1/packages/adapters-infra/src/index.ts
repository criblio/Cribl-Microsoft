// @soc/adapters-infra — InfraProvisioner: Bicep/Terraform for Azure + AWS labs.
// Implements core ports outside the pure core; real implementation lands in Phase 7-8.
// Shell for now (Phase 0 scaffold).
export const ADAPTER_ID = 'infra' as const;
