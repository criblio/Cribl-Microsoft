// @soc/adapters-azure — Microsoft Sentinel destination + Azure-native sources (SentinelClient/DcrDeployer/SchemaStore/PolicyClient via @azure/*).
// Implements core ports outside the pure core; real implementation lands in Phase 3-4.
// Shell for now (Phase 0 scaffold).
export const ADAPTER_ID = 'azure' as const;
