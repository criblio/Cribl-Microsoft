// @soc/adapters-ai — AiClient: Anthropic API (schema extraction/inference, AI-assisted Cribl pack generation).
// Implements core ports outside the pure core; real implementation lands in Phase 9.
// Shell for now (Phase 0 scaffold).
export const ADAPTER_ID = 'ai' as const;
