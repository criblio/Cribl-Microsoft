# CONTEXT: soc-optimizationtoolkit workspace

Purpose: consolidate the Cribl-Microsoft repository's capabilities (see docs/feature-catalog.md) into one maintainable product with two deployment targets sharing one codebase.

## Workspace map

- packages/core - domain logic + port interfaces. Zero IO, zero fetch, zero React. Vendored Cribl OpenAPI spec in assets/.
- packages/ui - shared React feature screens/components. Consumes ports via context; never performs IO directly.
- apps/cribl-app - Cloud shell. Vite build to .tgz for the Cribl App Platform (Cribl.Cloud only). Binds platform adapters: locked fetch, /kvstore, proxies.yml, policies.yml. Platform constraints documented in apps/cribl-app/AGENTS.md.
- apps/local-app - local shell for on-prem Cribl. Node host serves the same UI and fulfills the same ports (outbound HTTP, encrypted secrets, job scheduling). First run = onboarding GUI for both targets.

## Invariants (lint-enforced once packages have content)

1. core imports nothing from ui or apps. ui imports only core. Apps import both.
2. Every outbound call goes through a port client defined in core; each shell binds its own adapters.
3. Legacy code in the wider repo is a capability reference, never an implementation spec (redesign-first principle, see feature catalog). Exception: DCR/DCE name generation, production stream names/schemas, and pack naming are compatibility contracts preserved via characterization tests.
4. External-surface declarations (proxies.yml/policies.yml for cloud; local host allowlist) change in the same PR as the feature needing them.
5. No emojis anywhere (repo-wide rule).

## Status

Workspace restructured 2026-07-01. Phase 1 walking skeleton live in BOTH shells: onboardTable usecase + OnboardTableScreen over the six ports (in-memory fakes for tests); local Node host (loopback API, encrypted secrets, ARM/leader proxies) shipped 2026-07-03. Unit 1 app frame shipped: AUA gate, mode chooser, mode-filtered route table, settings screen, one budgeted status poller.

Unit 2 (Azure resource discovery and targeting) shipped 2026-07-03. Core: azure-discovery usecases - Enabled-only subscription list (legacy filter pinned), workspace list (name/RG/location/customerId/sku), resource-group choices with the VERBATIM deriveResourceGroupsFromWorkspaces fallback, create-RG / create-workspace (PerGB2018, 90-day retention, attempt-bounded provisioning poll), enable-Sentinel using the workspace's ACTUAL location (legacy always-eastus bug fixed + pinned), and commitTargetScope (merge into the active profile, never replace; browse NEVER commits). UI: AzureTargetingScreen in @soc/ui - always-visible selectors disabled-with-instructions before data, ONE loader effect (legacy had three overlapping), explicit "Use this target" commit surfacing invalidation consequences, offline free-text branch, RG-name sanitization (legacy rule mined), committed-scope chip in both frames' topBar. Shells: ARM nextLink pagination via AzureManagement.requestUrl - cloud adapter and local host route POST /api/azure/request-url, both hard-restricted to https://management.azure.com/ before any request (SSRF guard). The cloud harness's panel-4 discovery stays as diagnostics; the targeting screen is the product path.

Unit 3 (Logger port and in-app diagnostics) shipped 2026-07-03. Core (prior increment): Logger port with the structural hard rule (LogContextValue = string|number|boolean|null ONLY; redactedLength is the one sanctioned way to reference sensitive material), log-model (immutable ring append, PINNED formatLogLine format, filterLogEntries, buildSupportBundle), FakeLogger, and optional logger wiring in onboardTable/azure-discovery (absent logger = no-op). This increment: adapters, viewer, and shells. Cloud: PlatformLogger (500-entry in-memory ring, dev console mirror, warn/error lines mirrored fire-and-forget to ONE rolling plain KV entry, last 100, one PUT per warn/error). Local: browser HostLogger batches entries to POST /api/logs (20 entries or 3s, fire-and-forget); the host appends formatLogLine lines to data/logs/app.log (10MB cap, single .1 rollover - legacy rotation policy as prior art, reimplemented), serves GET /api/logs?tail=500, sanitizes every shipped entry server-side (non-primitive context values dropped), and logs its OWN events (API requests, ARM/Cribl token refreshes, upstream failures) through the same file - no secret or token value ever. UI: LogsScreen (requires 'none', registered in BOTH shells) with min-level/jobId/text filtering and a support-bundle download (logs + recent job records + shell platform facts) via ArtifactSink; @soc/ui parseLogLine round-trips the pinned line format so the local shell's file tail and the cloud shell's ring feed the same screen. UiPorts carries the logger, so usecases invoked with a shell's ports bundle log for free.

Unit 4 (Deployment and naming options as typed forms, ENG-43) shipped 2026-07-03. Core: domain/option-forms - OperationOptions (createDCE, skipExistingDCRs, deploymentTimeoutSeconds, templateOnly, keepTemplateVersions, customTableRetentionDays 30|90, dcePublicNetworkAccess, amplsResourceId) and CriblOptions (destinationPrefix 'MS-Sentinel-', destinationSuffix '-dest', workerGroup) as typed form DATA mined from the legacy param-forms.ts (the azure form is superseded by azure-profiles and NOT ported); field descriptors carry the legacy operational knowledge (30/64-char DCR name limits, Cribl 4.14+ for Direct, AMPLS implications); tolerant per-field parse codecs; validateOptions REJECTS non-numeric number input with named per-field errors (the legacy silent Number(val)||0 coercion is fixed, pinned by a test contrasting it); applyOptionsPatch is the merge-preserving save - unmanaged keys in the stored blob survive every save (pinned). UI: OptionsScreen (route 'options', requires 'none', registered in BOTH shells) renders both forms from the descriptors through ONE generic OptionFieldRow (the descriptor-driven pattern Units 5-7/20 reuse), InfoTip per field, validate-on-save per-field errors, dirty indicator, Save / Reset-to-defaults (staged, needs Save); pure decisions in options-state.ts with tests. Shells: ONE plain 'appOptions' entry (cloud KV / local host secrets, the appMode persistence pattern) via load/save callbacks passed to the screen; OnboardTableScreen takes optional criblDefaults - the persisted worker group preselects when it exists in the live list, and destination ids compose from the persisted prefix/suffix (destinationIdFromOptions, pinned against defaultSentinelDestinationId so the sanitize rule cannot drift).

Unit 5 (Custom _CL table creation, ENG-34 + custom path of ENG-33) shipped 2026-07-03. Core: domain/custom-table - parseTableSchemaFile accepts all three wild schema-file shapes (properties.schema.columns, properties.schema.tableDefinition.columns per the legacy-TS scan, and the PS engine's bare {columns}) with the legacy-TS missing-type -> string default; validateCustomTableSchema (exact _CL casing, non-empty columns, PS name/type rule, declared TimeGenerated must map to datetime); buildTablePutRequest reproduces the New-LogAnalyticsCustomTable ARM PUT verbatim (plan Analytics, retention 30/90 compatibility contract, api-version 2022-10-01, normalize through the characterized schema-mapping contract then strip the 13 Azure-managed reserved names). mapColumnType reconciled against the legacy-TS azure-deploy.ts copy (datetimeoffset/array additions adopted WITH source-documenting tests; the PS-derived characterization contract wins everywhere else). assets/vendor-schemas bundles the PS schema library verbatim (Cloudflare x2, CrowdStrike x10) as raw-text VENDOR_SCHEMAS so the air-gap path needs no fetch. onboardTable runs custom tables as the SAME pipelined job (catalog decision, no PS double-run): new create-custom-table step with GET-first idempotency (existing table's schema wins), 404 -> validate + PUT + attempt-bounded readback poll; onboardTableStepsFor drops the step for native tables so native job records stay byte-identical to the walking skeleton (pinned). UI: OnboardTableScreen reveals the custom section contextually for _CL names (native flow visually unchanged): schema source picker (vendor dropdown with typed-name preselect | upload/paste file | use-existing-table), monospace column preview (mapped types, injected TimeGenerated, stripped reserved names, inline validation errors), per-run 30/90 retention seeded from the persisted Unit 4 customTableRetentionDays (new operationDefaults prop threaded in BOTH shells); pure decisions in onboarding/custom-schema-state.ts with tests (source precedence, parse+preview derivation, retention default/override). GitHub-sourced schema auto-generation remains a post-Unit-14 delta.

Unit 6 (Batch deployment queue and DCE/Private Link modes, ENG-33 delta + ENG-39 inner loop) shipped 2026-07-03. Core: usecases/onboard-batch - onboardBatch wraps onboardTable per table (exactly ONE deploy implementation; the legacy pwsh-vs-powershell.exe drift does not port) as ONE parent 'onboard-batch' JobRecord: shared prologue (fetch-workspace; ensure-dce ONCE per batch, GET-first create-or-reuse via dcr-naming "dce-endpoint"; associate-ampls when DCE public access is disabled - idempotent, reused DCEs included), one 'table:{name}' step per table, per-table results embedded in the parent result and persisted after EVERY table (partial results, resumable; re-running a completed batch is a zero-call no-op finishing 'skipped'). JobStatus gained FIRST-CLASS 'skipped' (user decision): skip-existing hits, prerequisite-failed downstream skips (legacy error cascade fixed + pinned), resume skips. templateOnly fixed + pinned (legacy silently dropped the flag): zero ARM writes, all request bodies collected for artifact export. ARM budget pacing via paceAzureManagement over SHELL-INJECTED now/sleep hooks (poll-scheduler math, default 80 req/min); AMPLS-required-when-public-access-disabled cross-field validation landed in option-forms (deferred from Unit 4) AND in the usecase. UI: BatchDeployScreen in @soc/ui (route 'batch-onboard', requires 'both', BOTH shells) - free-text table list + add-from-vendor-schemas multi-select, persisted-options summary with tri-state per-run overrides (skipExistingDCRs/templateOnly/createDCE) and an open-Options hook, live per-table step lines with the distinct 'skipped' tag, combined deployed/skipped/failed summary, templateOnly ARM bodies downloaded as ONE JSON artifact via ArtifactSink, and batch records in the extended RecentRuns list; pure decisions in onboarding/batch/batch-state.ts with tests. Shells: both inject Date.now/setTimeout pacing hooks and the persisted Unit 4 options; no new external surface (management.azure.com already declared).

Unit 6.5 (Guided journey shell, ux-flow-plan) shipped 2026-07-04 in full: core + @soc/ui layer, then shell wiring in BOTH shells. Core: domain/journey-state - JourneyFacts models the SPLIT readiness facts (accepted, mode, identityPresent, secretLive 'live'|'unknown'|'missing', scopeCommitted, optional criblReachable), replacing the conflated five-field gate; deriveJourney returns BOTH arcs (FIRST_RUN_ARC accept/choose-mode/connect/target/ready mode-filtered per the plan contract - cribl-only omits target, air-gapped omits connect+target; INTEGRATE_ARC choose-content/configure/review/deploy/validate/monitor always in full, unshipped stages honestly 'not-yet-available'), nextAction (the single-next-action hint cascade as data), readinessChips (identity/secret/scope; 'unknown' never renders ok). READ-AHEAD contract (user decision, binding): every stage visible and navigable, gating ONLY at the commit actions already inside the screens; contract tests pin stage lists per mode, at-most-one 'current' mirrored by nextAction, blockedReason discipline, and the honesty rules across the full fact matrix. UI: styles.css refactored to CSS custom properties on :root mapping EXACTLY to the previous hardcoded light values (mapping table in the sheet header) with [data-theme='dark'] overrides (dark surfaces, readable muted text, status colors brightened, color-scheme flip; monospace results and disabled states legible in both); theme model split cleanly - core domain/app-theme owns the {"theme":...} codec (parse DEFAULTS to 'system', unlike appMode's null) and the ONE resolveTheme rule over the shell-read prefers-color-scheme, ui theme-state owns only APP_THEME_KEY 'appTheme', THEME_LABELS, the cycle order, and the toggle text (resolved theme always shown for 'system') - behind ThemeToggle in the frame topBar and the Settings Appearance select - ONE ThemeControl wiring, shells own persistence and the media query, the frame sets data-theme on its root wrapper from the shell-resolved theme; AppFrame nav gains SECTIONS (AppRoute.section 'journey'|'tools'|'diagnostics' defaulting 'tools', groupNavSections presentation-only AFTER the one filterNavItems pass); JourneyStepper (frame/journey-stepper.tsx) renders deriveJourney output with zero logic of its own - stage->route/hint binding lives in pure stepper-state (SHARED_JOURNEY_LINKS for bindings identical in both shells; 'connect' is deliberately per-shell props - cloud cross-links the Diagnostics harness panel 3 until Unit 9, local passes config-file guidance - never shell-sniffing prose; 'not-yet-available' never navigates, 'blocked' stays navigable per read-ahead); HomeScreen (screens/home, route id 'home', requires 'none', section 'journey') is the state-aware landing surface - readiness chips, ONE next-action card from the core cascade with the honest no-action fallback, both rails, the MODE_OPTIONS-sourced mode note with the Settings pointer, and RecentRuns embedded (mount inside a PortsProvider); pure decisions in home-state.ts/stepper-state.ts with tests. Recorded Unit 6.5 decisions: Home opens EVERY launch in both shells (initialRouteId 'home'); batch-onboard route requirement relaxes to 'azure' with templateOnly FORCED ON when the mode lacks a live Cribl connection. Shell wiring (both shells, same increment): initialRouteId 'home' with sectioned route tables - Journey (Home, Azure Targeting, Onboard, Batch Onboard in dependency order), Tools (Options, Logs, Settings), Diagnostics last (cloud only; the Spike Harness route retitled 'Diagnostics', panel 3 doubling as the Connect surface until Unit 9); JourneyFacts composed from signals each shell already owns - cloud: identity from the active profile, secretLive 'live' ONLY when this session connected the secret under the current identity (else the honest 'unknown'), scope from the committed profile fields, criblReachable from the consolidated platform-link poll; local: identity/scope from the host config + committed override, secretLive honestly 'unknown' (the file-held secret is never proven until a run); connect cross-links are per-shell JOURNEY_LINKS data (cloud routes to Diagnostics panel 3, local renders config-file guidance with no route) and the shared Onboard footer's harness prose became a shell-provided roleGuidance prop; theme choice persists per shell as the plain 'appTheme' entry (appMode pattern), resolved through core resolveTheme with a live prefers-color-scheme listener, data-theme set on <html> so gate screens re-theme too, ONE ThemeControl passed to AppFrame and Settings; App.css/local.css shell chrome (connection bar, notices, badges, headers, error banner) consume the shared styles.css tokens - zero hex duplicated, dark mode covers harness panels and change-request blocks; batch-onboard requires 'azure' in both shells with BatchDeployScreen's new forcedTemplateOnly prop (a mode FACT outranking the tri-state override - pure applyRunOverrides third arg, pinned by tests): templateOnly control disabled-with-reason (FORCED_TEMPLATE_ONLY_NOTICE), worker-group discovery skipped with the select visible-but-disabled, ingestion secret input disabled (no destination = no secret collected), Run no longer requires a group; the five-field gate panels point at Home/journey plus the renamed Diagnostics view with an Open Home action.

Unit 7 (Existing-resource check and deployment preview, ENG-36 + GUI-12, ux-flow-plan 5.2 REVIEW stage) shipped 2026-07-04. Core: usecases/deployment-preview - checkExistingDcrs (ONE live RG-scoped ARM DCR list at the pinned api-version 2023-03-11, paginated via the existing listAllPages/requestUrl, then ONE GET per MATCHED table - never for misses - resolving immutableId and the logs-ingestion endpoint through parseDcrDeployment with the dataCollectionEndpointId -> parseDceDeployment chain; endpoints returned VERBATIM - the handler.control rewrite stays a sentinel-destination concern) and buildDeploymentPreview (per table: dcr-naming's predicted name, the SAME builder request bodies a deploy run sends, workspace-table entry for _CL tables, existence flags; shared DCE entry in DCE mode; caller-supplied OPAQUE generatedAtToken echoed verbatim - core never reads a clock). DO-NOT-PORT defects fixed + pinned: the legacy preview's simplified name approximation is replaced by dcr-naming as THE single source (preview name === deployed name across the abbreviation-triggering legacy characterization vectors); existence truth is LIVE ARM only (the legacy preview trusted stale cached files while check-existing queried live); all four legacy fuzzy substring matchers replaced by exact full-name matching (shared-prefix collision tests: Cloudflare vs CloudflareAudit never cross-match); the legacy check-existing catch referenced an out-of-scope variable, so the whole handler was treated as uncharacterized - tests are written from the mining, never the code; previews are read-only by definition (all GETs, pinned by a call-method audit). UI: ReviewScreen in @soc/ui (route 'review', requires 'azure', section 'journey' after Batch Onboard, BOTH shells) - the Integrate arc's REVIEW stage: table entry through the SAME buildBatchSelection parse path as Batch Onboard (selection state is NOT lifted into a shared store this unit; REVIEW_SELECTION_NOTE says so honestly on screen), per-resource rows with DCR/TBL/DCE tags, Exists / Will create pills, verbatim detail lines, and expandable monospace request JSON; the STALENESS MARKER (shell-minted generated-at token + a pure inputs-token predicate over selection/options/scope - any change after generation flips a visible stale notice and relabels the action Re-check; nothing recomputes automatically); and the ACKNOWLEDGE GATE - a required 'I have reviewed these changes' check arming ONLY this screen's Deploy handoff button (navigates to Batch Onboard); the acknowledgement is TRANSIENT state, never persisted as consent, and review is never a hard gate on Deploy (read-ahead decision). Pure decisions in screens/review/review-state.ts with tests (row derivation, staleness predicate incl. acknowledged-but-stale disarms, handoff-arming cascade). Journey: UNSHIPPED_INTEGRATE_STAGES shrank to validate+monitor - 'review' is shipped and 'available' (read-ahead) in Azure modes; SHARED_JOURNEY_LINKS binds review -> route 'review' and deploy's hint now references the Review stage. Shells: both register the route, pass the module-scope REVIEW_GENERATED_AT supplier (the shell owns Date), and pass the journey deploy stage's blockedReason as the screen's disabled-control hint (journey-state data, never per-screen prose); no new external surface (management.azure.com GETs only).

Unit 11 UI (Sample intake + the Integrate page's Sample Data section, ENG-14/15/18, GUI-06 upload/paste/tag portions) shipped 2026-07-04. Core (prior increment): the sample-parsing domain (ONE content-based detectSampleFormat with strict/lenient modes, parseSampleContent with FIRST-CLASS Cribl-capture inner-_raw unwrap, ONE unified DISCRIMINATOR_FIELDS list, the ParsedSample/DiscoveredField/TaggedSample shared models, and the TaggedSampleStore port + FakeTaggedSampleStore). This increment: the section, its pure decision layer, the store adapters, and the integrate-arc flip. Core integrate-arc CHANGE (additive): sample-data flips built:false -> built:true (it renders real content now, no longer coming-soon), a new SectionInputs.samplesProvided boolean completes it and lights the Samples readiness pill (ok when a sample is tagged, muted coming-soon otherwise - NEVER missing, so it reads as optional), and canDeploy is UNCHANGED - samples do not gate the native-table deploy (the MVP-transition rule holds; the deploy the user validated live still runs with zero samples); integrate-arc + index tests updated (built count 3 -> 4, 2^5 input matrix). UI (@soc/ui): pure sample-intake-state.ts (chip derivation, field-table/raw-preview projections, buildTaggedSample capping stored records to RAW_EVENTS_CAP for KV/host size, dedupe-by-logType + upsert mirroring the store's replace semantics, and the rename RE-KEY primitives - renameInList for the tagged-sample list and reKeyByLogType<T> for downstream log-type-keyed edits, the fix for the legacy orphaning bug pinned in tests). SampleIntakeSection (screens/samples) renders inside the Integrate page's Sample Data NumberedSection: multi-file upload (browser File API, both shells, no upload endpoint) + paste-and-tag, per-sample CHIPS (detected-from-content format badge + event/field counts + guessed timestamp field), an expandable field table (name + inferred type + example) and raw preview, and a log-type RENAME that re-keys the tagged-sample store entry AND invokes the onRenameLogType contract so Unit 18 mapping edits re-key instead of orphaning. Format is ALWAYS detected from content (capture events unwrap to their inner _raw); a declared format is never trusted. IntegrateScreen wires the section to ports.samples, threads the tagged-sample count into deriveSectionInputs (sampleCount -> samplesProvided) so the arc completes Sample Data and lights the pill, and passes the rename contract (logged today; the seam Unit 18 re-keys through). Ports: UiPorts gains samples: TaggedSampleStore. Cloud adapter PlatformTaggedSampleStore = plain KV entries under tagged-samples/{encodedLogType} + a tagged-samples-index (mirrors PlatformJobStore; the core's 200-event rawEvents cap keeps each entry small). Local: new host module tagged-samples.mjs (data/tagged-samples.json, replace-by-logType in place, first-upsert order) + host routes GET/POST /api/tagged-samples and GET/DELETE /api/tagged-samples/{logType} (POST validates the TaggedSample shape) + LocalTaggedSampleStore over them - mirroring the secrets/jobs pattern. No new external surface (KV cloud-side, loopback host-side). Both shells' makeCloudPorts/makeLocalPorts bind the new store; the Integrate route already provides the full ports bundle. git check-ignore clean for all new files.

Integrate arc single-page flagship (legacy-flow-analysis.md structural decision ADOPTED 2026-07-04) shipped 2026-07-04 - THE MVP centerpiece. Core (prior increment): domain/integrate-arc - the pure model of the ONE numbered-section page (INTEGRATE_SECTIONS: solution/sample-data/azure-resources/cribl-config/gap-analysis/rule-coverage/deploy, numbered 1..7); the `built` flag is the single source of truth for the MVP built/not-built split (azure-resources/cribl-config/deploy BUILT NOW, the other four coming-soon with shippedInUnit 14/11/18/23); deriveSectionStatuses (read-ahead: at most one 'current'; not-built sections always 'coming-soon', never a working teaser), deriveReadinessPills (Solution/Samples/Mappings/Workspace/Worker Groups/Pack Name; the three content pills are honestly 'coming-soon', never false-ok), and canDeploy - the binding MVP-TRANSITION rule in ONE place: the operable native-table deploy needs ONLY the three built prerequisites (committed scope + worker group + pack name); the not-yet-built content prerequisites do NOT gate it (gating on them would break the flow the user validated live). Pure, no IO/fetch/React/Date/crypto; 28 tests. UI (@soc/ui, this increment): the REUSABLE numbered-section vocabulary later units reuse - NumberedSection (components/numbered-section.tsx: the numbered-circle badge - blue-filled number for current/available, green circle + CSS-drawn check for complete, muted/dashed for coming-soon; reuses the shared InfoTip; an AMBER gating-reason line when blocked; coming-soon renders an "Arrives in Unit N" note and description with NO body) and ReadinessFooter (components/readiness-footer.tsx: the sticky deploy-readiness pill row - green ok / muted coming-soon / amber missing from deriveReadinessPills - plus the single Deploy action enabled per canDeploy). IntegrateScreen (screens/integrate/integrate-screen.tsx, route 'integrate', requires 'both', section 'journey') renders the 7 NumberedSections in page order over @soc/core deriveSectionStatuses, composing the BUILT screens WITHOUT rewriting their logic: azure-resources embeds the AzureTargetingScreen subscription->workspace->RG cascade plus the DCE/metrics/role capability checkboxes (the role checkbox is Unit 8's target - rendered honestly disabled with the "assigns after deploy" note; DCE reflects the saved createDCE option); cribl-config is the worker-group select (listGroups, persisted-default preselect - the OnboardTableScreen pattern) + pack name (prefilled from the destination prefix via defaultPackName); deploy is the OPERABLE native-table onboard driving the SAME @soc/core onboardTable use-case (reusing onboardTableStepsFor/formatStepLine/summaryText/RecentRuns) with live step lines and the honest summary - the ReadinessFooter's Deploy triggers this run, so the user completes the validated native-table onboard end to end THROUGH this page. The four not-built sections render coming-soon. Pure decisions in screens/integrate/integrate-screen-state.ts with tests (deriveSectionInputs raw->SectionInputs reduction, defaultPackName prefill from the destination prefix, deployDisabledReason single-unlock hint; 11 tests). Shells (both, same increment): route 'integrate' registered as the PRIMARY journey route (first journey entry after Home) in cribl-app and local-app, wired with scopeCommitted from the composed JourneyFacts, handleCommitScope, the persisted Unit 4 cribl/operation options, and the roleGuidance/open-Options cross-links; full-mode JOURNEY_LINKS bind the integrate-arc stages choose-content/configure/deploy to the 'integrate' route (Home's Integrate rail opens the one page; 'review' stays bound to the dedicated Review screen; in cribl-only/air-gapped these stages are 'not-yet-available' and non-navigable, so the binding is inert). The standalone Onboard / Azure Targeting / Batch / Review routes stay registered - the Integrate page composes and supersedes them but they remain reachable during the transition. The validated native-table onboard is not broken; no new external surface. git check-ignore clean for all new files.
