# CONTEXT: cribl-app (Cloud shell)

Purpose: the Cribl App Platform target - a Vite/React SPA packaged as a .tgz and installed into a Cribl.Cloud leader, running in a sandboxed iframe.

Platform rules live in AGENTS.md (read it before touching fetch, navigation, or config): locked fetch with automatic auth, app-scoped KV store (encrypted entries are write-only from the client), proxies.yml external-domain declarations with server-side header injection, policies.yml product API grants, 30s proxy timeout, 100 req/min.

Boundaries: binds platform adapters to @soc/core ports and renders @soc/ui. App-specific code here should shrink toward wiring-only as core/ui fill in.

Key files: config/proxies.yml and config/policies.yml (admin-reviewed external surface - update in the same PR as the feature needing them), scripts/package.mjs (version bump + .tgz build), vite.config.ts (dev-mode config watcher + package endpoint).

Status: shared app frame wired in (porting-plan Unit 1). App.tsx now flows AuaGate -> ModeSelect -> AppFrame: acceptance and mode persist as PLAIN KV entries (auaAcceptance / appMode) through PlatformSecretsStore, parsed by the tolerant @soc/core codecs; resolveFramePhase keeps the gate from flashing for accepted users. The frame's routes are Onboard (requires both live sides), Spike Harness (diagnostics, always available, panels unchanged), and Settings (platform info incl. the consolidated poll's platform-link status, current mode + Reconfigure = write EMPTY_MODE_RECORD + reload, and the validate-before-save JSON editor over the active connection's non-secret config). The old view-switcher tabs are gone; the connection bar stays visible inside the frame above content. useConsolidatedPolling registers the single connection-status poll (KV key listing, 60s, budget-capped) - every future poll registers there, never its own setInterval. Onboard still builds makeCloudPorts(activeTenant) and mounts @soc/ui's OnboardTableScreen inside PortsProvider, gated behind the five-field completeness check; its incomplete-connection guidance now points scope selection at the Azure Targeting route (identity still comes from harness panel 3). Unit 2: the Azure Targeting route (requires: azure) mounts @soc/ui's AzureTargetingScreen; the explicit Use this target commit runs @soc/core commitTargetScope over the profile store (merge, never replace), applies the invalidation (scope changes only stale permission results; token/secret flags honored defensively), and surfaces the consequence through the connection bar's switchNotice, next to the new committed-scope chip (formatScopeChip). PlatformAzureManagement gained requestUrl for ARM nextLink pagination, HARD-restricted to https://management.azure.com/ before any fetch (same token flow + 401-retry-once; management.azure.com was already declared in proxies.yml). Panel-4 discovery in the harness stays as diagnostics - the targeting screen is the product path. Feature implementation follows the roadmap in ../../docs/feature-catalog.md.

Unit 3: platform/logger.ts adds PlatformLogger - the cloud Logger adapter: bounded in-memory ring (500 entries, @soc/core appendLogEntry), console mirror in dev only, and warn/error lines mirrored to the ONE rolling plain KV entry diagnosticsWarnLog (last 100 lines, one fire-and-forget PUT per warn/error on the established race-timeout fetchWithTimeout, serialized through a promise chain, seeded once from the stored entry so lines roll across sessions; every failure swallowed - logging never changes control flow). The adapter owns time (new Date().toISOString()); core stays clock-free. App.tsx holds ONE module-scoped instance so the ring survives connection switches, passes it into makeCloudPorts (CloudPorts now carries logger - usecases log for free), warns on connection-status poll failures, and registers the Logs route (requires 'none') mounting @soc/ui LogsScreen over the ring with shell platform facts for the support bundle. No new proxies.yml/policies.yml surface (KV only).

Unit 4: the Options route (requires 'none') mounts @soc/ui OptionsScreen over ONE plain KV entry appOptions, persisted through the same PlatformSecretsStore as appMode. App.tsx hydrates the parsed options alongside acceptance/mode on mount and passes loadOptions/saveOptions callbacks; saves are merged through @soc/core applyOptionsPatch (unmanaged blob keys survive) and refresh the shell's parsed copy, so the Onboard route's criblDefaults (worker-group preselect + destination-id prefix/suffix) follow immediately. No new proxies.yml/policies.yml surface (KV only).

Unit 6: the Batch Onboard route (requires 'both') mounts @soc/ui BatchDeployScreen behind the same five-field completeness gate as Onboard, inside PortsProvider over makeCloudPorts. The shell injects the module-scoped BATCH_PACING hooks (Date.now/setTimeout) that drive the usecase's rolling-minute ARM budget - the @soc/core default 80 req/min leaves headroom under the platform proxy's ~100 req/min for the status pollers - plus the persisted appOptions (operation defaults summarized on the screen, cribl defaults for worker-group preselect), and hands nav.navigate('options') as the open-Options hook. templateOnly artifacts download through the existing ArtifactSink adapter (browser Blob anchor). No new proxies.yml/policies.yml surface: batch traffic is the same management.azure.com + product-API set the single-table flow already uses.

2026-07-14: Diagnostics panels 3/4 promoted to the Setup page (home route) as @soc/ui AzureConnectSection / AzureResourcesSection + RepositoriesScreen, composed via HomeScreen setupSections inside the PortsProvider; connectAzure in App.tsx owns the azureBasic write + token verification and feeds handleSecretSaved/handleConnected. The harness keeps five renumbered pure-platform panels. Routes: options / azure-target / review REMOVED (Integrate composes the targeting cascade; ReviewScreen unused; appOptions persistence retained for Integrate's DCE toggle), preflight relabeled "Permission Verification". All panel-3/4 prose (walls, secret notices, ROLE_GUIDANCE, acquireToken error) now names the Setup sections.
