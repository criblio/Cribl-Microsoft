# CONTEXT: @soc/ui

Purpose: the shared React feature screens and components rendered by both shells (cribl-app iframe and local-app browser UI).

Boundaries: imports only @soc/core. No direct fetch or storage access; all IO flows through core ports provided via React context by the hosting shell.

Layout convention: one folder per feature domain from the catalog (onboarding, dcr, packs, discovery, governance, lookups, migration, drift, labs) plus frame/ (app chrome + gates), screens/ (settings), components/ (shared widgets), polling/ (the one budgeted poller).

Status: shared app frame + settings live (porting-plan Unit 1), joining the walking-skeleton onboarding screen. src/ports-context.ts defines PortsContext / PortsProvider / usePorts, carrying the six @soc/core port instances plus the active non-secret AzureConfig; onboarding/OnboardTableScreen drives the @soc/core onboardTable use-case (worker-group picker via cribl.listGroups, transient write-only-aware secret input, live step list from onProgress, honest summary and role-assignment guidance). frame/: AppFrame (route-table sidebar filtered by @soc/core filterNavItems, mode chip, topBar shell-chrome slot, content area), AuaGate (acceptance gate with rewritten platform AUA text, scroll-to-bottom enable, unscrollable-content escape), ModeSelect (four-mode first-run chooser), and frame-state.ts - the pure, tested layer: resolveFramePhase (loading contract: the gate NEVER flashes for an already-accepted user), EMPTY_MODE_RECORD (the Reconfigure-writes-empty-object contract), MODE_LABELS/MODE_OPTIONS (one label map, one chooser list), isScrolledToBottom. screens/: SettingsScreen (shell-provided platform info rows props bag, current mode + Reconfigure, and the validate-before-save raw-JSON editor over the active connection's non-secret config - optional, since the local shell's config is file-managed) with the pure validateConfigJson glue (parseAzureConfig underneath; refuses non-objects, surfaces dropped/coerced keys as warnings, never stores a pasted secret). components/InfoTip renders embedded newlines (pre-wrap) on hover/focus/click. polling/useConsolidatedPolling wraps the @soc/core poll-scheduler: the hook owns Date/timers, core owns the pure budget decisions; ALL status polls register here (only connection-status for now). screens/azure-targeting/ (porting-plan Unit 2): AzureTargetingScreen - the subscription -> workspace -> resource-group cascade over the @soc/core azure-discovery usecases with always-visible selectors (disabled-with-instructions before data), ONE loader effect fed by the pure buildLoaderPlan (the legacy page had three overlapping loaders), create-RG (legacy name-sanitization rule mined verbatim into sanitizeResourceGroupName) / create-workspace / enable-Sentinel actions with honest line-by-line output, an offline free-text branch (shell derives it from the frame mode), and browse-vs-commit separation: only the explicit "Use this target" button calls the shell's onCommitScope, whose invalidation consequence renders via commitNoticeText (connection-bar notice pattern); targeting-state.ts also exports formatScopeChip (the committed-scope topBar chip both shells render) and the tolerant parseTargetScope/serializeTargetScope codec the local shell persists its scope override with. Tests are pure and colocated (frame-state, config-json, step-line, targeting-state); DOM tests remain out of scope. Styling: src/styles.css is the single source of the class conventions (reset/body, panel, field, run-button, status, result, app-frame/mode-chip, gate/aua/mode-option, settings/config-editor, info-tip), exported as "@soc/ui/styles.css"; each shell imports it BEFORE its own shell-specific chrome stylesheet (cribl-app App.css, local-app local.css).

Unit 3 status: screens/logs/ added - LogsScreen (the shared diagnostics viewer, route requires 'none' in both shells): entries arrive through a shell-provided getRecentLogs() prop, filtering runs through @soc/core filterLogEntries (min level / exact jobId / grep-style text over the FORMATTED line), rendering through formatLogLine in a monospace block, Refresh re-reads the source, and Download support bundle composes @soc/core buildSupportBundle (fresh unfiltered entries + newest JobStore records capped at RECENT_JOBS_LIMIT + shell-provided primitive platformInfo) delivered via the ArtifactSink port. logs-state.ts is the pure tested layer: parseLogLine/logLineToEntry invert the PINNED core line format (tests build inputs with the REAL formatLogLine, so format drift fails here), buildLogFilter maps raw control values onto the core LogFilter. UiPorts gained an OPTIONAL logger, so a shell's ports bundle structurally satisfies usecase bundles like OnboardTablePorts (usecases log for free); AzureTargetingScreen passes ports.logger into every azure-discovery usecase call.

Unit 4 status: screens/options/ added - OptionsScreen (route 'options', requires 'none' in both shells) renders the two @soc/core option forms (operation, cribl) from their OptionFormField descriptors through ONE generic OptionFieldRow (label + InfoTip from the descriptor's description + control by kind + inline per-field error) - the descriptor-driven pattern Units 5-7/20 reuse. Validation runs ON SAVE through the @soc/core validateOptions contract (non-numeric number input rejects with a named error; nothing is silently coerced to 0 like the legacy Number(val)||0); a dirty indicator marks unsaved edits; Reset to defaults stages the defaults and still requires Save. Persistence is shell-owned via loadOptions/saveOptions callbacks over ONE stored blob; the screen keeps the RAW stored string so saves flow through applyOptionsPatch and unmanaged keys survive. options-state.ts is the pure tested layer (stateFromOptions / defaultOptionsState / isOptionsStateDirty / validateOptionsState with 'formId.key' error keys / patchFromState). OnboardTableScreen gained optional criblDefaults (the persisted worker group preselects when present in the live list; destination ids compose via destinationIdFromOptions) - absent, legacy behavior is unchanged.

Unit 5 status: onboarding/custom-schema-state.ts added - the pure decision layer behind OnboardTableScreen's custom (_CL) section, which reveals CONTEXTUALLY when the typed table name routes custom (isCustomTableName; native names render the screen visually unchanged). deriveCustomSchemaPreview enforces source precedence (exactly one active source; the others' inputs are ignored, pinned): "vendor" (bundled VENDOR_SCHEMAS dropdown, preselected via defaultVendorIdForTable when the typed name matches an entry), "file" (upload via the browser File API or paste; all three wild shapes through @soc/core parseTableSchemaFile), "existing" (no schema sent; the workspace table's live schema wins). The derivation yields column preview rows (declared type + mapped type via the characterized mapColumnType, injected-TimeGenerated and stripped-reserved-column flags computed with the REAL core functions, never a second list), blocking errors (parse failures + validateCustomTableSchema rules, rendered inline), non-blocking warnings (table-name mismatch, unknown types defaulting to string), and ready/providesSchema gates the Run button consumes; formatSchemaPreview renders the monospace preview table. Retention: resolveRetentionDays - a per-run 30/90 override wins, otherwise the persisted Unit 4 default (new operationDefaults prop; "" no-override sentinel keeps late-loading options effective). The run passes customSchema + customTableRetentionDays into onboardTable only for custom jobs; the step list seeds from onboardTableStepsFor so create-custom-table appears automatically. The old refused-_CL hint is rewritten: _CL now IS the custom path.

Unit 6 status: onboarding/batch/ added - BatchDeployScreen (route 'batch-onboard', requires 'both' in both shells) drives the @soc/core onboardBatch usecase: table multi-select as a free-text list (newline/comma-separated, exact-dedupe) PLUS add-from-vendor-schemas (bundled VENDOR_SCHEMAS _CL entries carry their schema through the SAME parse path as an upload; a pick whose table is also typed MERGES the schema into the typed entry); a summary of the persisted Unit 4 OperationOptions with an open-Options navigation hook and tri-state per-run overrides for skipExistingDCRs/templateOnly/createDCE (saved options never change); Run seeds the parent step lines from onboardBatchStepsFor (shared prologue + one table:{name} line per table) and renders live onProgress updates - 'skipped' renders with its distinct first-class tag through the existing formatStepLine contract; the combined summary derives deployed/skipped/failed counts plus aligned per-table outcome lines (templateOnly runs say "templates collected", never "deployed"); templateOnly runs deliver ALL collected ARM request bodies as ONE deterministic JSON artifact via the ArtifactSink port (auto-saved once, re-downloadable). The screen takes SHELL-INJECTED BatchPacing (now/sleep) - the package never reads a clock for the ARM budget. RecentRuns gained optional kind/title/label/detail props (defaults preserve the onboard-table behavior) so batch records reuse the same persisted-run list via batchRunLabel/batchRunDetail. All decisions live in the pure batch/batch-state.ts with tests (selection parsing + merge rules, override application, the AMPLS cross-field re-check over EFFECTIVE options through the real validateOptions - a per-run createDCE override cannot sneak past the Unit 6 rule, counts derivation, summary formatting, artifact naming/serialization, record label/detail tolerance).

2026-07-14 status: Setup sections added under screens/setup-wizard/ - AzureConnectSection (shell-injected onConnect returning {ok, secretStored, message}; storageNote prose is shell-supplied), AzureResourcesSection (discovery + az role script + RBAC effective-action validation over ports.azure/ports.secrets/ports.artifacts; auto-discovers on connectNonce; bootstrap subscription input when discovery returns zero), ChangeRequestBlock (ArtifactSink download), useRunner, and the pure azure-setup-state module (parsers, armFailureMessage, permissionScopeChecks, evaluateScopeLines, storedCredentialReport - pinned in azure-setup-state.test.ts). HomeScreen renders an optional setupSections node between the journey rails and the mode note. SHARED_JOURNEY_LINKS rebound: target -> home, ready -> preflight, review unbound (standalone screen retired from nav). screens/options/ DELETED (Options route retired in both shells; @soc/core option-forms and the appOptions codecs are untouched).

2026-07-16 status: screens/labs/ added - LabsScreen (route 'labs', Tools section, requires 'azure' in both shells) drives roadmap Phase 5's first slice: profile picker over the @soc/core LAB_PROFILES (8 UnifiedLab presets) with public/private mode, the two resource-group permission modes (create-new vs bring-your-own per the feature-catalog permission design), target/naming/TTL inputs with live validation from buildLabPlan, the plan review (gated phases, planned resource names filtered to what the profile deploys, permission matrix, warnings, plan JSON via the ArtifactSink), and the live foundation deploy through the provisionLabFoundation usecase (three step lines from onProgress; honest result summary including the manual az grant command when the role step fails). The shell's optional mintAssignmentName gates the deploy button with a wiring-gap reason when absent (same convention as role assignment). labs-state.ts is the pure tested layer (form defaults, form -> LabPlanInput with strict integer parsing - junk never coerces to 0 - deploy gating, phase/name/result formatting, deterministic plan artifact, injected-nowIso TTL expiry preview).

2026-07-16 status, second wave: LabsScreen deploys phases 1-3 live through the @soc/core provisionLab engine - the step list pre-seeds from initialLabSteps (provisionLabStepsFor, so only the profile's phases render), onProgress updates in place, and labRunResultLines (formerly foundationResultLines) reports storage (account with collision-suffix note, containers, queues, Event Grid topic + subscriptions) and networking (NSGs, VNet) outcomes with created-vs-existed honesty. The screen mints the storage collision suffix locally (Math.random in the impure component layer; core never sees randomness).

2026-07-17 status: LabsScreen deploys ALL ten lab phases. New controls: the transient VM admin password (shown only for VM profiles; gates the deploy button via vmPasswordMissing), the optional on-premises VPN block (device IP, comma-separated CIDRs, transient shared key -> onPremFromForm; all-blank means the connection step self-reports skipped), and the post-run "Download Cribl configs" button (criblBundleArtifact via ArtifactSink). labRunResultLines covers every phase outcome (AMPLS, EH/ADX, flow logs, VMs, per-DCR lines with immutable ids and ingestion endpoints, gateway readiness, bundle summary with required secrets).

2026-07-17 status, permission preflight: the Labs plan-review panel gains "Check permissions" - checkLabPermissions over ports.azure rendered through the pure permissionCheckLines ([OK]/[MISSING] per profile-derived action, the TTL-grant ABAC verdict with the remediation text, notes). Informational like the RBAC preflight; the deploy button is not gated on it.

2026-07-17 status, flow-log pack + inventory: LabsScreen gains three surfaces - (1) a subscription selector (listSubscriptions on mount, free-text fallback) feeding the plan, permission check, deploy, and inventory; (2) the "Subscription and running labs" panel: refreshable lab inventory rows via formatLabInventoryRow with per-lab Extend TTL and a TWO-STEP confirmed Destroy (Destroy... -> CONFIRM destroy {name}); (3) the "Deploy the vNet flow-log pack" panel: worker-group select via ports.cribl.listGroups, storage-account override (defaults to the lab's deployed account), transient client-secret input, schedule toggle, install via ports.packInstall + finalizeFlowLogPack, rendered through flowLogPackResultLines. Gated honestly on the optional packInstall port.

2026-07-28 status: (1) ArchitectureFlow nodes carry an "i" button opening a CSP-safe popover (no portal, nodrag/nopan) with the node's purpose and vendor documentation links from @soc/core diagramNodeInfo; the canvas grew from a fixed 460px to clamp(460px, 68vh, 760px). (2) JourneyLink gains optional anchorId and Home's next-action button scrolls to it after navigating - the fix for same-route actions (the cloud shell's connect stage routes to the Setup page itself; the button previously appeared to do nothing). The cloud shell anchors connect at setup-connect-section above its AzureConnectSection.

2026-07-29 status: LabsScreen split into per-feature panels (maintainability audit): the screen is now a THIN composer owning only the shared state (form + derived plan, subscription selection, last deploy result) over LabInventoryPanel (list/extend/two-step destroy; extend-hours validation moved to the pure parseExtendHours), LabProvisionerPanels (profile / target+TTL / plan review + permission check / deploy; owns only its IO state), and FlowLogPackPanel (pack assembly + install; storage-account default via packStorageAccountName - typed > deployed > planned, ONE precedence function used by both the screen default and the panel). labs-state gains labTtlFromForm as the ONE TTL parse: the deploy call now uses the SAME strict integer parse as the plan preview (the raw Number() second path is gone).

2026-07-29 status, architecture screen: a third picker - "Specific log sources (optional)" over @soc/core LOG_SOURCES - feeds recommendPatterns' sources dimension; hints explain the role-split Event Hub / Blob Storage entries and that the six Windows entries are the same data over different collection methods (each draws a different ingress edge). ArchitectureFlow's destination-column badge now derives from the node label (Sentinel / Cribl / Azure / Destination) - the fixed "Sentinel" badge wrongly branded Cribl Lake, ADX, and downstream consumers (user report 2026-07-29).

2026-07-29 status, architecture page full build-out: (1) arch-layout.ts owns the dagre geometry + nodeBadge (renderer-agnostic; the flow canvas is a thin adapter) so architecture-svg-export.ts reproduces the EXACT on-screen coordinates - a pure, deterministic, XML-escaped standalone-SVG exporter with a hardcoded LIGHT palette (kept in sync with the styles.css tokens by comment); png-export.ts rasterizes it at 2x via offscreen canvas (thin DOM glue, untested by design). (2) The screen gains preset chips (ARCHITECTURE_PRESETS, order-insensitive active highlight, Clear), the product capability legend strip, a fourth picker over SOLUTION_INGESTION_ENTRIES (offline, hint = tier + kind), the cost legend line, Download SVG / Download PNG buttons, and per-pattern deploy buttons. (3) Deploy bindings live in ui-layer deploy-links.ts (PATTERN_DEPLOY_LINKS - the SHARED_JOURNEY_LINKS precedent; pinned against the shared 13-route list); AppFrameNav gains canNavigate(routeId) so the button HIDES with an honest hint when the active mode has no Azure connection instead of navigating into the frame fallback. ArchitectureScreen takes optional { onNavigate, canNavigate, onExport } - propless rendering still works; both shells pass nav + their ArtifactSink.

2026-07-29 status, Live Architecture tab: the screen gains Reference patterns / Live architecture tabs (arch-view-tab pills). The Live view (renders when the new optional cribl prop is present; both shells pass their CriblClient) lists Stream worker groups (isStreamWorkerGroup filter, FlowLogPackPanel error copy), fetches the group's real configuration via fetchLiveArchitecture, and renders buildLiveDiagram's result on the SAME canvas - drag/bend/delete/export all work on the live flow; an Azure-related-only toggle (default on) re-filters without re-fetching; honesty notes render under the canvas. ExportRow and CostLegend extracted as shared components (exports carry a per-view base name). ArchNodeCard prefers a node's inline info (live nodes) over the label lookup; arch-layout passes info through; nodeBadge treats "log analytics" labels as Azure. Cloud policies.yml gained the four read-only GETs.

2026-07-29 status, Dataflow rename + overlay/badge rendering: the section is user-facing "Dataflow" everywhere (nav label, page titles, tab "Live dataflow", button copy, export base names dataflow-diagram / live-dataflow-{group}); the internal route id stays 'architecture' so navigation references hold. Rendering: LaidOutNode/ArchNodeData pass through the node's badge (live stage captions override nodeBadge) and overlays; ArchNodeCard draws overlays as solid arch-flow-overlay-tag pills overlapping the card's bottom-right corner (the Sentinel-on-workspace representation, user directive 2026-07-29) and the SVG exporter mirrors both (badge override + corner tags) so exports match the canvas. The screen applies @soc/core applySentinelOverlay to the unified patterns diagram; cloud policies.yml gained the /m/{gid}/jobs GET for the Live view's collector-job section.

2026-07-29 status, Live view navigation + focus chips: ArchitectureScreen gains criblUiBase (optional, like cribl) passed into buildLiveDiagram so live popovers deep-link into the Cribl UI; the popover links heading renamed "Documentation" -> "Links". The cloud shell derives the base from the EMBEDDING page's origin (document.referrer - the app iframes inside the workspace UI - falling back to its own origin) + /stream; the local shell derives it from the /api/config criblLeaderUrl via @soc/core criblUiBaseFromLeaderUrl. The Live panel adds "Show only these sources / destinations" chip rows (arch-preset-chip grammar) built from the UNFOCUSED build's endpoint nodes, feeding focusSources/focusOutputs; selections reset per snapshot; "Show all flows" clears.

2026-07-29 fix, cloud criblUiBase derivation: the app iframe sits on a SANDBOXED origin whose server answers every path with the app itself, so the earlier own-origin fallback made "Open in Cribl" links boot a fresh app copy at the AUA gate (user report). The cloud shell now derives the workspace origin from location.ancestorOrigins[0] (the embedder - ground truth where supported), else the referrer origin ONLY when it differs from our own, else undefined - in which case live popovers keep their documentation links instead of emitting broken ones.

2026-07-29 status, edge tone rendering: the canvas pipe gains arch-flow-pipe-tone-search (violet #9254de, declared after the cost classes so the tone wins on edges carrying both) and the SVG exporter mirrors it (lineStroke: tone beats cost for the LINE; the PREMIUM caption keeps the warn color). CostLegend adds the violet swatch line "Cribl Search send path - findings return through Stream".

2026-07-30 status, flow list + explode button: the Live panel's source/destination chips are REPLACED by the flow-inventory checkbox list (liveResult.flows, azureOnly narrows the list to drawable rows; nothing checked = draw everything; "Show all flows" clears) - one build call now serves both the list and the canvas. ArchNodeCard renders an accent +/- toggle at the card's bottom-left for expandable nodes (ArchitectureFlow.onToggleNodeExpand; the screen keeps expandedPacks state and rebuilds); selections and expansions reset per snapshot.

2026-07-30 status, routes explode rendering + label wrap: the +/- toggle now also sits on the Routes hub (toggleNodeExpand handles "routes" and "pack:{id}"; routesExpanded state resets per snapshot). Muted nodes/edges render subdued (opacity classes; muted edges skip the animateMotion packets - a disabled route moves nothing) on canvas and SVG (group opacity / stroke-opacity). Long unbroken Cribl ids wrap instead of overhanging the card (user report: Reduction_Zscaler_Internet_firewall): .arch-flow-node-label gains overflow-wrap anywhere, and the SVG splitLabel now breaks at _ / - / . nearest the middle and hard-splits tokens with no break character at all.

2026-07-30 status, popover rows + flow dropdown: ArchNodeCard renders info.facts as label/value rows (92px faint label column), info.steps as numbered accent pills, and info.routes as scaled-down route-table bubbles (pipeline chip -> destination chip, filter tooltip, disabled dimmed). The Live panel's flow inventory moved from a checkbox LIST into the shared SearchableMultiSelect dropdown (22-flow groups were eating the screen - user report); "Show all flows" still clears.

2026-07-30 fix, readable popovers on muted cards (user directive, applied across the board): .arch-flow-node-muted no longer sets opacity on the CARD (the popover is a child and inherited the 50% - unreadable); the muted look is now a dashed faint border plus per-child dimming that EXEMPTS the info popover and every interactive control (info/explode/remove buttons stay full-strength). Popover-internal disabled route rows dim to 0.6 instead of 0.5 - subdued, never illegible.

2026-07-30 fix, label/card overlap (user report): edge labels are now DECLARED to dagre (setEdge width/height/labelpos from the label length, wrapped footprint over 150px) so the layout reserves a collision-free virtual-node slot per label - ranks widen exactly where a label needs room, not everywhere; LaidOutEdge gains labelPoint (dagre's reserved spot). The SVG exporter anchors labels at labelPoint (fallback: path midpoint), wraps long labels onto two lines via splitLabel with a properly sized backing rect, and hangs the cost caption under the label block. The canvas pill wraps at max-width 150px (white-space normal) and the bend dot drops to +24 so two-line pills clear it. PINNED: across every preset's merged diagram, no reserved label box intersects any card (arch-layout.test.ts).

2026-07-30 fix, canvas overlap + window efficiency (user screenshot: cost-reduction preset - PREMIUM badge on the Auxiliary card, labels on crossings, a thin wide ribbon): the CANVAS now draws dagre's ROUTED geometry by default, exactly like the SVG export - roundedOrthogonalPath over the routed waypoints with endpoints re-pinned to the live handle coords (dragged nodes stay connected), labels/cost badges anchored at dagre's reserved labelPoint (fallback: shared polylineMidpoint, moved to arch-layout); smoothstep remains only the no-routing fallback and the drag-bend override. Width reclaimed: edgeLabelFootprint (ONE shared rule: cap 132px, two-line wrap, +14px when a cost badge stacks under the label) + ranksep 72->56 (dagre halves it around its label ranks). The overlap regression pin now also checks LABEL-vs-LABEL boxes across every preset merge.

2026-07-30 fix, ribbon layouts + off-screen fit (user reports: long-term-retention ran off screen; dead bands above/below): (1) FIT RACE - FitViewController fits on DOUBLE rAF plus a 200ms backup so React Flow has measured newly seeded nodes before bounds are computed (a single rAF could fit the STALE diagram). (2) SERPENTINE WRAP - layoutDiagram gates on ASPECT (width > 1200 AND > 3.4x height -> compact 40/22 pass; still > 3.0x -> serpentineWrap): rank columns fold into 2-3 left-to-right rows, each row translating RIGIDLY (intra-row routed paths and reserved label spots stay valid); cross-row connectors snake orthogonally through the gap band above the lower row, each gap SIZED from the label boxes it carries with per-gap lane cursors (a fixed 90px band overflowed five crossings into the next row's cards), and carry wrap=true so the canvas keeps normal left/right handles. PINNED: every preset merge lays out under 4.6:1 aspect with zero label-card and label-label collisions (failure messages carry coordinates).

2026-07-30 status, Reset diagram button (user request): the canvas overlay now always shows "Reset diagram" (both views) - clears removals, re-seeds nodes/edges from the pristine layout (undoing drags), and bumps a remount key on the canvas subtree, which is what clears the per-edge bend state and re-runs the initial fit; the "Restore N removed items" chip stays alongside for removals-only undo.

2026-07-30 status, diagramming best-practices pass (user directive "do it all", grounded in graph-drawing aesthetics research - crossings/direction/overlap/progressive disclosure were already covered): (1) ARROWHEADS on every canvas edge (MarkerType.ArrowClosed, color follows the pipe: cost/tone tinted, muted faint) - direction reads at a glance and in static captures, not just via the packet animation. (2) COMPLETE LEGEND: subdued ("configured but not flowing") and "(copy)" rows join premium/economical/search. (3) TITLED EXPORTS: diagramToSvg(diagram, {title}) renders a title block (screen passes view + date) and EVERY non-empty export embeds a legend strip; node groups now tag <g data-node>. (4) STAGE BANDS: computeTierBands derives captioned per-tier background strips from the dominant tier of each rank column (descriptive, never a constraint; only when >=2 bands, single-row layouts only) - drawn via ViewportPortal underlay on canvas and rects in SVG. (5) VIEWPORT-AWARE WRAP: layoutDiagram(diagram, targetAspect) - the canvas feeds its measured aspect (ResizeObserver, rounded 0.1) so serpentine folds toward the real window shape; SVG keeps the 2.4 default. (6) ROW DIVIDERS: dashed midlines in serpentine gaps (canvas + SVG) as the continuation cue. (7) NODE BUDGET nudges: >25 components hints at presets/removals (patterns) or the flow inventory (live). Pins: bands derived + wrapped layouts swap bands for dividers; exports carry title/legend/band captions.

2026-07-30 fix, serpentine min-cut + margin bus (user screenshot: the Splunk migration wrap crossed lines everywhere): the fold line is now chosen by MINIMUM EDGE CUT over width-balanced candidate boundaries (2-row single boundary; 3-row pairs when >=5 columns; crossings first, aspect distance tiebreak), and when even the best cut severs more than 5 edges the graph is branchy, not a chain - it stays unwrapped (a wide clean ribbon beats spaghetti). Cross-row connectors ride a RIGHT-MARGIN BUS: exit to a per-edge lane right of all content, descend outside everything, run leftward along the gap band, enter the target from its left - no more mid-diagram drops through row content. Width accounts for the lanes; all geometry pins (label-card, label-label, preset aspect) hold.

2026-07-30 fix, folds must earn their row + per-row height (user screenshot: a min cut isolated "Log sources" into its own band and every row floated inside the full ribbon height): (1) fold plans now require GENUINE balance (2-row spans >= 0.45 of the max, 3-row >= 0.32) AND real width savings (max row span <= 0.75x / 0.55x of the unwrapped width) - a cut that saves no width never wins on crossings alone. (2) Per-row vertical normalization: each row shifts up by its content min-y and is only as tall as its own content; rows stack with exactly the gap bands between (rowTop from cumulative rowHeight + gapHeight). All geometry pins hold.

2026-07-30 fix, shortest-clear-path cross connectors (user report: lines swept the full width when a short hop existed): cross-row edges take the DIRECT hop (down beside the source, along the gap band, up into the target - the gap run spans only source-to-target columns) whenever BOTH vertical corridors are clear of cards (corridorClear rect test with 6px margin); only blocked corridors fall back to the right-margin bus lane. Label anchors follow the chosen route; all geometry pins hold.

2026-07-30 status, multi-bend edges (user request "more points on the line to move smaller sections"): FlowingEdge holds bends: FlowPoint[] instead of one bend - SOLID dots mark each bend (drag moves it, double-click removes it), HOLLOW dashed dots sit at the midpoints between consecutive anchors (or at the label anchor while unbent) and dragging one INSERTS a bend at that spot, immediately draggable via pointer capture. orthogonalThrough generalized to orthogonalVia (L-jog through every via in order, same exit/enter axis handling, near-collinear collapse); the label anchor follows the bent route midpoint; snapping guides include the endpoints AND the neighboring bends. Reset diagram still clears all bends via the remount key.

2026-07-30 status, canvas ergonomics slice (user directive "do all of them", then live corrections): NEW arch-edits.ts holds the ONE edit model - DiagramEditState {positions, edges: {bends, labelOffset}, removedNodes, removedEdges, notes} - with localStorage load/save (best-effort, empty state clears the key). (1) EXPORTS RESPECT EDITS: diagramToSvg(diagram, {title, edits}) applies removals, dragged positions (bands drop once cards moved), bend polylines drawn exactly as on canvas, label offsets, and sticky-note annotations (pinned). (2) PERSISTENCE: ArchitectureFlow takes storageKey (patterns: sorted selection; live: group+azure toggle) - arrangements survive reloads; no key = session-only. (3) UNDO/REDO: Ctrl+Z / Ctrl+Y|Ctrl+Shift+Z over snapshot-on-gesture-start history (cap 50; skips inputs/textareas; Reset is undoable). (4) BOX SELECT: Shift-drag selection, Ctrl/Meta multi-select, native group drag. (5) SNAP-GRID: 12px on node drags. (6) DRAGGABLE LABELS: the label pill drags (offset in the edit model; double-click snaps back). (7) NOTES: Add note button (stacked UNDER Reset diagram), draggable sticky cards, double-click edits, x deletes (always faintly visible on notes - the hover-gate was scoped to .arch-flow-node only), Delete key works too. BENDS REWORKED mid-slice (user: via-points forced new right angles): bends now store the INTERIOR CORNERS of an orthogonal polyline and every grab dot is a SEGMENT handle - dragging slides that segment perpendicular while neighbors stretch (endpoint-adjacent segments grow a 24px stub corner), releasing merges collinear corners so dropping a section in line straightens it and a fully straight line returns to the automatic route; FlowingEdge is fully CONTROLLED (bends/labelOffset live upstream so undo/persist/export see them); orthogonalVia deleted.

2026-07-31 status, Edge fleets view (user directive): the Dataflow screen grows a THIRD tab "Edge fleets" - EdgeFleetsPanel loads fetchEdgeFleetData once (no group picker; every reported fleet renders), builds a FleetInventory per fleet (parseWorkerInventory + buildFleetInventory with the criblUiBase), and renders one .arch-fleet card per fleet: offload summary chips ("{output} ({type}) offloads to Stream worker group X"; unresolved receiver hosts shown raw with "(unresolved)", none configured stated plainly), the flow inventory list with the resolved offload group APPENDED to each group-flow row (the list answers the question without opening the diagram), the shared ArchitectureFlow canvas (storageKey fleet:{id}; per-fleet plain-object edits holders - the fleet COUNT is data-driven so useRef cannot own them - feeding titled exports), per-fleet notes, and honest hints for zero fleets, the fetch cap (skipped ids listed), and a failed worker-inventory read.

2026-07-31 status, node-category color coding (user directive; replaces chasing ever-smoother layout as the readability lever): four visually distinct category colors across canvas, bands, and exports - sources gold, routes violet, pipelines/packs magenta, destinations green (existing), with Cribl-blue and Azure-cyan unchanged for the rest. New --tier-source/--tier-route/--tier-pipeline tokens in BOTH theme blocks; .arch-flow-node-{source,route,pipeline} card styles (source upgraded from faint gray to gold) with tinted badges; TIER_BADGE/BAND_LABEL/TIER_ORDER cover the new tiers (stage bands can now caption "Routes"/"Pipelines" columns); the SVG exporter mirrors the light tokens in TIER_STYLE and its embedded legend gains four node-category SWATCH rows (bordered card chips, distinct from edge line samples) with width-aware wrapping (legend height is now layout-driven - long legends wrap instead of overflowing narrow artifacts); CostLegend leads with the same four swatches.
