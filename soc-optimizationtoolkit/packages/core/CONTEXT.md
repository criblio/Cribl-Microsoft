# CONTEXT: @soc/core

Purpose: pure domain logic and the port interfaces both app shells bind against.

Boundaries: imports nothing from ui or apps. Zero IO, zero fetch, zero React. Everything here must be unit-testable with plain vitest and fakes.

Invariants:
- Port interfaces are the only seam between shared code and the two shells (cloud platform adapters vs local Node host).
- DCR/DCE name generation must reproduce legacy output exactly (compatibility contract; characterization tests mandatory).
- assets/cribl-openapi.json is the vendored Cribl API spec (pinned per Cribl version); typed API client shapes are written against it.

Status: Phase 1 foundation in place. src/ports/ defines the six port interfaces (SecretsStore, UserContext, ArtifactSink, JobStore, AzureManagement, CriblClient) plus shared HTTP types; src/testing/ provides in-memory fakes for all six; src/domain/dcr-naming/ and src/domain/schema-mapping/ implement the legacy compatibility contracts with characterization tests pinned to legacy vectors/fixtures. src/usecases/azure-discovery/ (porting-plan Unit 2) provides listSubscriptions (Enabled-only, pinned), listWorkspaces, listResourceGroupChoices (verbatim deriveResourceGroupsFromWorkspaces fallback), createResourceGroup, createWorkspace (attempt-bounded provisioning poll), enableSentinel (workspace's ACTUAL location - legacy eastus bug fixed + pinned), listAllPages (nextLink pagination via the OPTIONAL AzureManagement.requestUrl, single-page fallback without it), and commitTargetScope (merge through azure-profiles + connection-invalidation). All are re-exported from src/index.ts (including AzureManagementUrlRequest from ports/). Remaining domain modules (pipeline-generation, field-matcher, pack-assembly, reduction-rules, sample-parsing) and the GraphClient port land per the implementation roadmap.

Unit 4 (porting-plan): src/domain/option-forms/ - deployment and naming options as typed form DATA mined from the legacy param-forms.ts (azure form NOT ported - superseded by azure-profiles). OperationOptions + CriblOptions with DEFAULTS; OptionFormField descriptor lists whose descriptions carry the legacy operational knowledge (30/64-char DCR name limits, Cribl 4.14+ for Direct, AMPLS implications); tolerant per-field parse codecs (parseAppOptions/parseOperationOptions/parseCriblOptions, junk -> that field's default) plus canonical serializeAppOptions; validateOptions REJECTS non-numeric number input with named per-field errors (DECISION pinned by test: the legacy `Number(val)||0` silently persisted 0); formValuesToOptions/optionsToFormValues for descriptor-driven renderers; immutable getNestedValue/setNestedValue; applyOptionsPatch - the MERGE-PRESERVING save (unmanaged keys in the stored blob survive, pinned by test); destinationIdFromOptions (pinned against defaultSentinelDestinationId so the duplicated sanitize rule cannot drift).

Unit 5 (porting-plan): src/domain/custom-table/ - custom (_CL) Log Analytics table creation as data: parseTableSchemaFile accepts all three wild schema-file shapes (properties.schema.columns, properties.schema.tableDefinition.columns, bare {columns}) with the legacy-TS missing-type -> string default; validateCustomTableSchema (exact _CL suffix - the case-insensitive isCustomTableName routes "foo_cl" to the custom path where validation rejects it instead of the legacy silent double-suffix; PS name/type column rule as named errors; declared TimeGenerated must map to datetime); buildTablePutRequest (the New-LogAnalyticsCustomTable PUT verbatim: plan Analytics, retention 30/90 contract, api-version 2022-10-01, normalize via schema-mapping then strip the 13 reserved names, strippedReservedColumns reported). schema-mapping's mapColumnType RECONCILED against the legacy-TS azure-deploy.ts copy (datetimeoffset/array additions documented with source, PS contract wins elsewhere; mapcolumntype-reconciliation.test.ts). assets/vendor-schemas/ bundles the PS engine's schema files verbatim (Cloudflare x2, CrowdStrike x10) as raw-text VENDOR_SCHEMAS entries so the picker exercises the same parse path as an upload. usecases/onboard-table: custom (_CL) tables run the SAME pipelined job - new create-custom-table step (GET-first idempotency, existing schema wins; 404 requires customSchema, PUT + attempt-bounded readback poll), onboardTableStepsFor(table) drops the step for native tables (native job records stay byte-identical to the walking skeleton, pinned), customTableRetentionDays 30|90 feeds the retention contract.

Unit 6 (porting-plan): src/usecases/onboard-batch/ - onboardBatch wraps onboardTable per table as ONE parent 'onboard-batch' JobRecord (exactly ONE deploy implementation; the legacy repo's two drifted ones do not port): shared prologue steps (fetch-workspace; ensure-dce ONCE per batch via GET-first create-or-reuse with dcr-naming mode "dce-endpoint" over the workspace name - the legacy per-table DCE quota burn is a recorded deviation; associate-ampls when public network access is disabled, ALWAYS applied idempotently, reused DCEs included) plus one 'table:{name}' step per table, per-table child results EMBEDDED in the parent result and re-persisted after EVERY table (partial results, resumability - a re-run of a fully completed batch issues ZERO ARM/Cribl calls and finishes 'skipped'). JobStatus/step status gained FIRST-CLASS 'skipped' (user decision 2026-07-03): skip-existing hits (GET-first, zero deploy calls), downstream tables of a failed prologue step (reason "prerequisite-failed" - the legacy cascade defect fixed + pinned), and resume skips. templateOnly ACTUALLY works (legacy accepted-and-dropped defect fixed + pinned by a zero-ARM-write call-count test): every ARM body a deploy would PUT is collected into result.templates for the SHELL to export via ArtifactSink, with the predicted DCE resource id (real subscription) wired into DCE-based DCR bodies. Budget pacing: paceAzureManagement wraps the port with poll-scheduler rolling-minute math over SHELL-INJECTED now()/sleep() hooks (BatchPacing; default 80 req/min) - core stays clock-free; polling maps deploymentTimeoutSeconds onto attempt bounds (pollAttemptsForTimeout, 10s/attempt). The Unit 6 AMPLS cross-field rule (deferred from Unit 4) lands in option-forms validateOptions (createDCE + public access disabled REQUIRES a well-formed amplsResourceId; the legacy warned and shipped an unreachable private-only DCE) AND is re-enforced in the usecase for programmatic input. onboardTable accepts an optional preresolved dce input (DCE-mode children: dcr-naming mode "dce", buildDceDcrRequest body, destination on the DCE's logsIngestion endpoint); domain/dce-request builds/parses the DCE PUT and the AMPLS scopedResources association.

2026-07-14 status: THREE additions this wave. (1) domain/field-matcher/derive-schema-from-sample.ts - deriveCustomTableSchema derives a _CL destination schema from the ParsedSample when the catalog resolves nothing (CCF solutions), seeded by content-referenced column names under canonical casing; analyzeSamples consumes it (AnalyzeSamplesInput.contentColumnNames) and GapReport gained optional schemaDerivation {summary, notes}. (2) domain/coverage-analysis ContentRequirements gained optional columnNames (lowercased -> first-seen ORIGINAL casing; derive + merge populate it) so schema derivation can create columns rules/workbooks actually query. (3) domain/siem-migration + usecases/siem-migration (porting-plan Unit 26): the legacy analyzer's knowledge bases verbatim as data assets, Splunk JSON + RFC-4180 QRadar CSV parsers, identifyDataSources with same-solution merging and the ONE normalizeSourceKey (the legacy dual-regex unmapped-count bug, fixed + pinned), pure applyFuzzySolutionMap, MITRE rollup, assembleMigrationPlan (persisted rawSearch capped at MIGRATION_RAW_SEARCH_CAP), enrichPlanWithAnalyticRules (pure fold), generateMigrationReport (HTML, injected date), and the analyzeSiemExport usecase (SentinelContent-driven fuzzy tier + analytics-rule enrichment, capped 20 solutions / 40 rule files, degrades gracefully without the port).

2026-07-16 status (roadmap Phase 5, first slice): src/domain/labs/ - the lab domain ported from the legacy UnifiedLab: lab-naming (LAB-13: location-suffix application, prefix+base+suffix composition, storage/ADX special rules - the ADX uniqueness hash is a RECORDED DEVIATION, pinned FNV-1a replacing the non-portable .NET GetHashCode - full planned-name map, RG name composition, plus the legacy default naming/subnet/hub/SKU data verbatim); lab-validation (LAB-14 pure validators: CIDR, subnet containment + pairwise overlap via uint32 range math, storage name rules, EH partition bounds 1-32, ADX SKU whitelist with the Dev cost warning, settings/placeholder checks; the legacy LIVE permission check does not port - permission-preflight covers it); lab-profiles (the 8 Get-LabDeploymentConfig presets verbatim including private-mode conditionals and ResourceGroupSuffix; the 10-phase model with the Storage-before-Networking order and Test-PhaseRequired gating verbatim); lab-foundation (LAB-02: foundation/TTL tag set with tag names verbatim, TTL math on an INJECTED nowIso in real UTC - the legacy local-time-with-Z bug fixed + pinned - the TTL watchdog Logic App workflow definition verbatim as a direct ARM PUT, RG GET/PUT/PATCH builders, the Contributor role-assignment builder); lab-plan (buildLabPlan: flags + gated phases + names + component-scoped validation + the two-mode permission matrix from the feature-catalog permission design). usecases/provision-lab: provisionLabFoundation - the three-step foundation job (kind 'provision-lab-foundation'): resource-group (GET-first; create-new PUTs with the mandatory TTL tags, an existing group gets tags MERGED and PATCHed - "TTL extended"; bring-your-own REQUIRES existence, never creates), ttl-logic-app (GET-first PUT; identity principal readback attempt-bounded through the injected sleep), ttl-role-assignment (shell-minted GUID, 409 RoleAssignmentExists = success, PrincipalNotFound attempt-bounded retries, terminal failure carries manualLabRoleCommand - the az CLI grant for the least-privilege bring-your-own path). Failed prerequisites SKIP downstream steps (first-class 'skipped'). TTL is MANDATORY by construction: no tag builder or usecase path exists without an expiration.

2026-07-16 status, second wave (roadmap Phase 5 slice 2): domain/labs gains lab-storage (LAB-04/LAB-05: the legacy StorageV2/Standard_LRS/Hot account settings, the SHELL-minted collision-suffix retry shape (base truncated to 20 + 4 chars, capped 24), containersToDeploy with the VERBATIM per-profile skip rules (flowlogs only with flow logs, criblqueuesource only WITH Event Grid, criblblobcollector only WITHOUT), management-plane container/queue builders (no storage keys - a recorded deviation from the legacy data-plane cmdlets; the unused legacy queue messageTTL is dropped, not implied), and the Event Grid wiring: provider registration builders, the {account}-events system topic, eg-sub-{key} subscriptions with includedEventTypes + subject filters) and lab-networking (LAB-03: the verbatim AllowOnPremises_{100+i}/AllowAzureServices(120, AzureCloud) NSG rule set with placeholder/malformed on-prem CIDRs FILTERED instead of failing the NSG, and buildVnetPutRequest - ONE desired-state PUT carrying the full subnet set with inline NSG associations, replacing the legacy remove-stale/add-missing/associate three-pass; NSGs deploy BEFORE the VNet, a recorded order flip with the same end state). usecases/provision-lab: provisionLabFoundation superseded by provisionLab (kind 'provision-lab') - the phased engine whose step list provisionLabStepsFor includes only the phases the profile requires (foundation always; storage/networking per isLabPhaseRequired). Cross-phase semantics: a TTL watchdog/grant failure skips ALL later phases (the mandate - no billable lab resources without a working self-destruct); a storage-account failure skips its dependent sub-steps while the independent networking phase still runs; sub-steps a profile does not request report 'skipped' with "not requested by profile". Storage/VNet PUTs poll provisioningState attempt-bounded through the injected sleep; the storage collision retry and all randomness stay SHELL-owned (mintStorageSuffix).

2026-07-16 status, third wave (roadmap Phase 5 slice 3): usecases/provision-lab gains phase 4 Monitoring - COMPOSED from the existing azure-discovery usecases, zero new ARM logic: 'log-analytics' (GET-first; a missing workspace runs createWorkspace with the legacy PerGB2018/90-day defaults and its attempt-bounded provisioning poll) and 'microsoft-sentinel' (enableSentinel's idempotent SecurityInsights pre-check + PUT at the workspace's ACTUAL location; skipped 'prerequisite-failed' when the workspace step failed). provisionLabStepsFor appends the monitoring steps when isLabPhaseRequired(4); a private-mode monitoring profile additionally carries a 'private-link' step always reported skipped ("not implemented in-app yet") so the pending AMPLS work is visible in every job record instead of silently absent. ProvisionLabResult gains monitoring {workspaceName, workspaceCreated, sentinelEnabled, sentinelAlreadyEnabled}. The DEFAULT SentinelLab (public) profile is now deployable end-to-end from the Labs screen: foundation + workspace + Sentinel.
