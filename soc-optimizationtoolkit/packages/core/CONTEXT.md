# CONTEXT: @soc/core

Purpose: pure domain logic and the port interfaces both app shells bind against.

Boundaries: imports nothing from ui or apps. Zero IO, zero fetch, zero React. Everything here must be unit-testable with plain vitest and fakes.

Invariants:
- Port interfaces are the only seam between shared code and the two shells (cloud platform adapters vs local Node host).
- DCR/DCE name generation must reproduce legacy output exactly (compatibility contract; characterization tests mandatory).
- assets/cribl-openapi.json is the vendored Cribl API spec (pinned per Cribl version); typed API client shapes are written against it.

Status: Phase 1 foundation in place. src/ports/ defines the six port interfaces (SecretsStore, UserContext, ArtifactSink, JobStore, AzureManagement, CriblClient) plus shared HTTP types; src/testing/ provides in-memory fakes for all six; src/domain/dcr-naming/ and src/domain/schema-mapping/ implement the legacy compatibility contracts with characterization tests pinned to legacy vectors/fixtures. src/usecases/azure-discovery/ (porting-plan Unit 2) provides listSubscriptions (Enabled-only, pinned), listWorkspaces, listResourceGroupChoices (verbatim deriveResourceGroupsFromWorkspaces fallback), createResourceGroup, createWorkspace (attempt-bounded provisioning poll), enableSentinel (workspace's ACTUAL location - legacy eastus bug fixed + pinned), listAllPages (nextLink pagination via the OPTIONAL AzureManagement.requestUrl, single-page fallback without it), and commitTargetScope (merge through azure-profiles + connection-invalidation). All are re-exported from src/index.ts (including AzureManagementUrlRequest from ports/). Remaining domain modules (pipeline-generation, field-matcher, pack-assembly, reduction-rules, sample-parsing) and the GraphClient port land per the implementation roadmap.

Unit 4 (porting-plan): src/domain/option-forms/ - deployment and naming options as typed form DATA mined from the legacy param-forms.ts (azure form NOT ported - superseded by azure-profiles). OperationOptions + CriblOptions with DEFAULTS; OptionFormField descriptor lists whose descriptions carry the legacy operational knowledge (30/64-char DCR name limits, Cribl 4.14+ for Direct, AMPLS implications); tolerant per-field parse codecs (parseAppOptions/parseOperationOptions/parseCriblOptions, junk -> that field's default) plus canonical serializeAppOptions; validateOptions REJECTS non-numeric number input with named per-field errors (DECISION pinned by test: the legacy `Number(val)||0` silently persisted 0); formValuesToOptions/optionsToFormValues for descriptor-driven renderers; immutable getNestedValue/setNestedValue; applyOptionsPatch - the MERGE-PRESERVING save (unmanaged keys in the stored blob survive, pinned by test); destinationIdFromOptions (pinned against defaultSentinelDestinationId so the duplicated sanitize rule cannot drift).

Unit 5 (porting-plan): src/domain/custom-table/ - custom (_CL) Log Analytics table creation as data: parseTableSchemaFile accepts all three wild schema-file shapes (properties.schema.columns, properties.schema.tableDefinition.columns, bare {columns}) with the legacy-TS missing-type -> string default; validateCustomTableSchema (exact _CL suffix - the case-insensitive isCustomTableName routes "foo_cl" to the custom path where validation rejects it instead of the legacy silent double-suffix; PS name/type column rule as named errors; declared TimeGenerated must map to datetime); buildTablePutRequest (the New-LogAnalyticsCustomTable PUT verbatim: plan Analytics, retention 30/90 contract, api-version 2022-10-01, normalize via schema-mapping then strip the 13 reserved names, strippedReservedColumns reported). schema-mapping's mapColumnType RECONCILED against the legacy-TS azure-deploy.ts copy (datetimeoffset/array additions documented with source, PS contract wins elsewhere; mapcolumntype-reconciliation.test.ts). assets/vendor-schemas/ bundles the PS engine's schema files verbatim (Cloudflare x2, CrowdStrike x10) as raw-text VENDOR_SCHEMAS entries so the picker exercises the same parse path as an upload. usecases/onboard-table: custom (_CL) tables run the SAME pipelined job - new create-custom-table step (GET-first idempotency, existing schema wins; 404 requires customSchema, PUT + attempt-bounded readback poll), onboardTableStepsFor(table) drops the step for native tables (native job records stay byte-identical to the walking skeleton, pinned), customTableRetentionDays 30|90 feeds the retention contract.

Unit 6 (porting-plan): src/usecases/onboard-batch/ - onboardBatch wraps onboardTable per table as ONE parent 'onboard-batch' JobRecord (exactly ONE deploy implementation; the legacy repo's two drifted ones do not port): shared prologue steps (fetch-workspace; ensure-dce ONCE per batch via GET-first create-or-reuse with dcr-naming mode "dce-endpoint" over the workspace name - the legacy per-table DCE quota burn is a recorded deviation; associate-ampls when public network access is disabled, ALWAYS applied idempotently, reused DCEs included) plus one 'table:{name}' step per table, per-table child results EMBEDDED in the parent result and re-persisted after EVERY table (partial results, resumability - a re-run of a fully completed batch issues ZERO ARM/Cribl calls and finishes 'skipped'). JobStatus/step status gained FIRST-CLASS 'skipped' (user decision 2026-07-03): skip-existing hits (GET-first, zero deploy calls), downstream tables of a failed prologue step (reason "prerequisite-failed" - the legacy cascade defect fixed + pinned), and resume skips. templateOnly ACTUALLY works (legacy accepted-and-dropped defect fixed + pinned by a zero-ARM-write call-count test): every ARM body a deploy would PUT is collected into result.templates for the SHELL to export via ArtifactSink, with the predicted DCE resource id (real subscription) wired into DCE-based DCR bodies. Budget pacing: paceAzureManagement wraps the port with poll-scheduler rolling-minute math over SHELL-INJECTED now()/sleep() hooks (BatchPacing; default 80 req/min) - core stays clock-free; polling maps deploymentTimeoutSeconds onto attempt bounds (pollAttemptsForTimeout, 10s/attempt). The Unit 6 AMPLS cross-field rule (deferred from Unit 4) lands in option-forms validateOptions (createDCE + public access disabled REQUIRES a well-formed amplsResourceId; the legacy warned and shipped an unreachable private-only DCE) AND is re-enforced in the usecase for programmatic input. onboardTable accepts an optional preresolved dce input (DCE-mode children: dcr-naming mode "dce", buildDceDcrRequest body, destination on the DCE's logsIngestion endpoint); domain/dce-request builds/parses the DCE PUT and the AMPLS scopedResources association.

2026-07-14 status: THREE additions this wave. (1) domain/field-matcher/derive-schema-from-sample.ts - deriveCustomTableSchema derives a _CL destination schema from the ParsedSample when the catalog resolves nothing (CCF solutions), seeded by content-referenced column names under canonical casing; analyzeSamples consumes it (AnalyzeSamplesInput.contentColumnNames) and GapReport gained optional schemaDerivation {summary, notes}. (2) domain/coverage-analysis ContentRequirements gained optional columnNames (lowercased -> first-seen ORIGINAL casing; derive + merge populate it) so schema derivation can create columns rules/workbooks actually query. (3) domain/siem-migration + usecases/siem-migration (porting-plan Unit 26): the legacy analyzer's knowledge bases verbatim as data assets, Splunk JSON + RFC-4180 QRadar CSV parsers, identifyDataSources with same-solution merging and the ONE normalizeSourceKey (the legacy dual-regex unmapped-count bug, fixed + pinned), pure applyFuzzySolutionMap, MITRE rollup, assembleMigrationPlan (persisted rawSearch capped at MIGRATION_RAW_SEARCH_CAP), enrichPlanWithAnalyticRules (pure fold), generateMigrationReport (HTML, injected date), and the analyzeSiemExport usecase (SentinelContent-driven fuzzy tier + analytics-rule enrichment, capped 20 solutions / 40 rule files, degrades gracefully without the port).

2026-07-16 status (roadmap Phase 5, first slice): src/domain/labs/ - the lab domain ported from the legacy UnifiedLab: lab-naming (LAB-13: location-suffix application, prefix+base+suffix composition, storage/ADX special rules - the ADX uniqueness hash is a RECORDED DEVIATION, pinned FNV-1a replacing the non-portable .NET GetHashCode - full planned-name map, RG name composition, plus the legacy default naming/subnet/hub/SKU data verbatim); lab-validation (LAB-14 pure validators: CIDR, subnet containment + pairwise overlap via uint32 range math, storage name rules, EH partition bounds 1-32, ADX SKU whitelist with the Dev cost warning, settings/placeholder checks; the legacy LIVE permission check does not port - permission-preflight covers it); lab-profiles (the 8 Get-LabDeploymentConfig presets verbatim including private-mode conditionals and ResourceGroupSuffix; the 10-phase model with the Storage-before-Networking order and Test-PhaseRequired gating verbatim); lab-foundation (LAB-02: foundation/TTL tag set with tag names verbatim, TTL math on an INJECTED nowIso in real UTC - the legacy local-time-with-Z bug fixed + pinned - the TTL watchdog Logic App workflow definition verbatim as a direct ARM PUT, RG GET/PUT/PATCH builders, the Contributor role-assignment builder); lab-plan (buildLabPlan: flags + gated phases + names + component-scoped validation + the two-mode permission matrix from the feature-catalog permission design). usecases/provision-lab: provisionLabFoundation - the three-step foundation job (kind 'provision-lab-foundation'): resource-group (GET-first; create-new PUTs with the mandatory TTL tags, an existing group gets tags MERGED and PATCHed - "TTL extended"; bring-your-own REQUIRES existence, never creates), ttl-logic-app (GET-first PUT; identity principal readback attempt-bounded through the injected sleep), ttl-role-assignment (shell-minted GUID, 409 RoleAssignmentExists = success, PrincipalNotFound attempt-bounded retries, terminal failure carries manualLabRoleCommand - the az CLI grant for the least-privilege bring-your-own path). Failed prerequisites SKIP downstream steps (first-class 'skipped'). TTL is MANDATORY by construction: no tag builder or usecase path exists without an expiration.

2026-07-16 status, second wave (roadmap Phase 5 slice 2): domain/labs gains lab-storage (LAB-04/LAB-05: the legacy StorageV2/Standard_LRS/Hot account settings, the SHELL-minted collision-suffix retry shape (base truncated to 20 + 4 chars, capped 24), containersToDeploy with the VERBATIM per-profile skip rules (flowlogs only with flow logs, criblqueuesource only WITH Event Grid, criblblobcollector only WITHOUT), management-plane container/queue builders (no storage keys - a recorded deviation from the legacy data-plane cmdlets; the unused legacy queue messageTTL is dropped, not implied), and the Event Grid wiring: provider registration builders, the {account}-events system topic, eg-sub-{key} subscriptions with includedEventTypes + subject filters) and lab-networking (LAB-03: the verbatim AllowOnPremises_{100+i}/AllowAzureServices(120, AzureCloud) NSG rule set with placeholder/malformed on-prem CIDRs FILTERED instead of failing the NSG, and buildVnetPutRequest - ONE desired-state PUT carrying the full subnet set with inline NSG associations, replacing the legacy remove-stale/add-missing/associate three-pass; NSGs deploy BEFORE the VNet, a recorded order flip with the same end state). usecases/provision-lab: provisionLabFoundation superseded by provisionLab (kind 'provision-lab') - the phased engine whose step list provisionLabStepsFor includes only the phases the profile requires (foundation always; storage/networking per isLabPhaseRequired). Cross-phase semantics: a TTL watchdog/grant failure skips ALL later phases (the mandate - no billable lab resources without a working self-destruct); a storage-account failure skips its dependent sub-steps while the independent networking phase still runs; sub-steps a profile does not request report 'skipped' with "not requested by profile". Storage/VNet PUTs poll provisioningState attempt-bounded through the injected sleep; the storage collision retry and all randomness stay SHELL-owned (mintStorageSuffix).

2026-07-16 status, third wave (roadmap Phase 5 slice 3): usecases/provision-lab gains phase 4 Monitoring - COMPOSED from the existing azure-discovery usecases, zero new ARM logic: 'log-analytics' (GET-first; a missing workspace runs createWorkspace with the legacy PerGB2018/90-day defaults and its attempt-bounded provisioning poll) and 'microsoft-sentinel' (enableSentinel's idempotent SecurityInsights pre-check + PUT at the workspace's ACTUAL location; skipped 'prerequisite-failed' when the workspace step failed). provisionLabStepsFor appends the monitoring steps when isLabPhaseRequired(4); a private-mode monitoring profile additionally carries a 'private-link' step always reported skipped ("not implemented in-app yet") so the pending AMPLS work is visible in every job record instead of silently absent. ProvisionLabResult gains monitoring {workspaceName, workspaceCreated, sentinelEnabled, sentinelAlreadyEnabled}. The DEFAULT SentinelLab (public) profile is now deployable end-to-end from the Labs screen: foundation + workspace + Sentinel.

2026-07-17 status (roadmap Phase 5, all phases): domain/labs completes the UnifiedLab port with six modules - lab-privatelink (AMPLS/scoped-workspace/private-endpoint/DNS builders; the legacy dead DNS-zone config gate fixed, AMPLS accessModeSettings Open/Open for the 2021-07-01-preview api), lab-analytics (EH namespace/hub/consumer-group builders at 2021-11-01; Kusto cluster/database/script builders at 2023-08-15 with the verbatim 159-column CommonSecurityLog schema and .create-table composition), lab-flowlogs (watcher resolution constants, FlowLog-{vnet}[-{subnet}] names, dual-level retention defaults, the CannotCreateMoreThanOneFlowLogPerTargetResource idempotent-hit detector), lab-compute (the {base}-{vmName} legacy naming quirk, NIC/VM/devtestlab-schedule builders, transient-password VM PUT), lab-gateway (Standard/Static PIP deviation, gwIpConfig gateway PUT, lng-onprem/conn-azure-to-onprem builders, isOnPremConnectionConfigured placeholder filter), lab-cribl (the five legacy generator shapes verbatim + buildLabCriblBundle with the per-pattern gating, reusing the discovery buildEventHubSourceConfig). usecases/provision-lab runs all phases in the legacy order with per-phase outcomes on ProvisionLabResult (privateLink/analytics/flowLogs/compute/dcrs/criblConfigs/gateway); phase 8 composes selectSchemaColumns + generateDcrName + buildDirectDcrRequest + parseDcrDeployment directly (LAB-10's cross-script glue disappears), gated on the workspace step; phase 9 is PURE assembly into the result; long provisioning polls (ADX/VPN/VM) ride longPollAttempts (default 270) with honest re-run-to-resume exhaustion failures.

2026-07-17 status, permission preflight (user request after a live ABAC 403): domain/labs/lab-permissions - labRequiredActions derives the control-plane action list from the SAME component flags and phase gating the deploy uses (no drift), foundation actions always (RG create only in create-new mode); analyzeRoleAssignmentGrant reads the ABAC condition strings off the raw RBAC permissions response and classifies HOW roleAssignments/write is held (unconditional / conditional-allows-contributor / conditional-blocks-contributor via a stated best-effort Contributor-GUID text check / not-granted) - the constrained-RBAC-Administrator case where effective-action evaluation alone says "granted" but the live TTL grant fails AuthorizationFailed("ABAC condition that is not fulfilled"). usecases/provision-lab/check-lab-permissions: ONE permissions GET at the mode's scope (create-new: subscription; bring-your-own: the RG, falling back to subscription with a note), evaluates via the EXISTING hasEffectiveAction, downgrades the roleAssignments/write check row when the condition blocks Contributor, and carries ROLE_CONDITION_REMEDIATION. The engine's ttl-role-assignment failure now appends the targeted ABAC explanation (add Contributor to the condition's allowed roles for service principals) when the 403 body names an ABAC condition.

2026-07-17 status, flow-log pack + inventory: domain/labs gains flowlog-pack-assets (AUTO-EXTRACTED verbatim breaker/pipeline/manifest/logo from the legacy pack source), lab-flowlog-pack (renderFlowLogJobsYml - the VNF-11 collector job with real values where the legacy shipped "<replace me>"; assembleFlowLogPack building the deterministic .crbl through the SAME PackTree/buildCrbl machinery as every pack; the VNF-10 Redis pipeline and VNF-12 samples are documented omissions pending parameterization/scrub), and lab-inventory (tag-driven lab identification - this app's ManagedBy, legacy UnifiedAzureLab, bare TTL_Enabled - with injected-now expiry math and soonest-first sorting). usecases/provision-lab gains manage-labs (listLabs via paginated listAllPages; extendLabTtl re-stamping foundation TTL tags over existing tags, preserving the recorded TTL_UserEmail when the caller passes none; destroyLab reporting the async 202 as ACCEPTED) and deploy-flowlog-pack (finalizeFlowLogPack: optional Azure_vNet_Flowlogs_Secret create-or-update from transient input, then commit+deploy with the onboard-table REPORTED-BUT-NONFATAL semantics).

2026-07-28 status: architecture-patterns corrections + node info (user review). The public-path patterns (event-hub-fanin, edge-fleet, vnet-flow-collection, entra-reroute's new ingestion node) now show Kind:Direct DCR - a plain "DCR" wrongly implied a DCE-less non-Direct rule - so the unified canvas merges them with direct-dcr's node (pinned); private-ingestion models the Data Collection Endpoint AND the DCE-based DCR as distinct nodes (stream -> DCE -> DCR -> workspace), with a fan-in consideration stating the DCE is only needed when Private Link is in play. NEW diagramNodeInfo(label): purpose text + Microsoft Learn / docs.cribl.io links per node, keyed by the SAME canonical label key the merge uses; a completeness test pins that every catalog node label resolves an entry with valid vendor URLs.

2026-07-29 status: architecture deepening (maintainability audit, all four candidates executed). (1) usecases/arm-http.ts is the ONE home for the ARM/HTTP idioms (is2xx/prop/asString/httpErrorText/armErrorCode/isErrorCode/mergedTags) previously copy-pasted across a dozen usecase files and hand-inlined in check-lab-permissions/manage-labs; exact-copy consolidation, contract pinned by arm-http.test.ts; update-dcr keeps its stricter local prop (excludes arrays) on purpose. (2) provisionLab decomposed: provision-lab.ts is now the SEQUENCER and public facade (~220 lines; re-exports provision-lab-types.ts unchanged) over ten phase modules (foundation/storage/networking/monitoring/analytics/flowlogs/compute/dcr/cribl/gateway-phase.ts) behind the LabPhaseContext seam (ports + resolved retry knobs + shared result/errors + step bookkeeping; foundation is the ONE aborting phase - the TTL mandate). The repeated shapes live once in provision-lab/arm-resource.ts: ensureResource (GET-first ensure-or-create; strict "404" and lenient "any-non-2xx" miss modes - both legacy behaviors preserved per resource) and pollProvisioningState (attempt-bounded; seeded sleep-first vs unseeded read-first loops; keepStateOnFailedRead for the ADX/DCR variant; onBody for the ADX URI capture; exhaustion MESSAGES stay with callers). All 25 engine tests passed unchanged as characterization pins; arm-resource.test.ts pins the toolkit seam directly.

2026-07-29 status, architecture patterns expansion (user directives): domain/architecture-patterns grows three dimensions. (1) SENTINEL DATA LAKE: new resource (implies sentinel + log-analytics in expandResources) with two patterns - sentinel-data-lake-tiering (Stream -> DCR -> workspace analytics tier -> lake via tier mirroring, single copy; lake-only tier tables as the cost lever) and search-sentinel-data-lake (Cribl Search federates over the lake's OWN KQL query endpoint api.securityplatform.microsoft.com/lake/kql/v1/rest/query - pinned as DISTINCT from the Log Analytics Query API api.loganalytics.io; async /lake/kql/jobs for long-running queries; no dedicated Search provider yet - use an API-based provider). (2) LOG SOURCES: new LogSource dimension (LOG_SOURCES catalog, ArchitectureSelection.sources, ArchitecturePattern.requiresSources) with seven ingress variants - Palo Alto NGFW syslog (PAN pack -> CommonSecurityLog) and SIX Windows collection methods (WEF-to-Stream with Stream as the WEC and the ~22-EventID XPath cap noted; WEC server relay with Edge on the collector; Cribl Edge local WEL; Winlogbeat via the Elasticsearch API source; Splunk UF via Splunk TCP S2S; Azure Monitor Agent direct with its AMA-managed DCR - the honest no-Cribl comparison path). Source-ingress patterns NEVER surface unless their source is selected (pinned). (3) ROLE-SPLIT RESOURCES: Event Hub and Blob Storage are listed per role (source into Cribl vs Stream destination) since both sides pair with Stream; ADX is ONE entry disambiguated by product (adx-destination for Stream queued/streaming ingest, adx-search-in-place via the native ADX dataset provider) - stream+search+adx merges into the land-then-search loop with one ADX node (pinned). vnet-flow-collection now requires only vnet-flow-logs (storage implied). DIAGRAM_NODE_INFO covers every new label (doc-link test extended to allow packs.cribl.io); the literal NUL byte in the unify edge-key template is now written as its escape sequence so the file reads as text again.

2026-07-29 status, live architecture + honest layering (user directives): (1) LIVE ARCHITECTURE - new domain/live-architecture: buildLiveDiagram turns a six-section config snapshot of a real worker group (/system/inputs, /system/outputs, /routes, /pipelines, /lib/breakers, /packs - fetched by usecases/live-architecture fetchLiveArchitecture via Promise.allSettled) into a PatternDiagram: tolerant envelope parsing, route-filter attribution (__inputId forms; unrecognized filters over-approximate to any-input with a note), ONE Routes hub node, per-route egress through pack:/pipeline/passthru into destinations with default-output resolution and cribl_lake synthesis, non-final "(copy)" labels, disabled-element skips, azureOnly flow filtering (a kept flow keeps ALL its nodes), pinned cost tiers, and per-node DiagramNodeInfo composed from real config (DiagramNode gained optional info; the popover prefers it). Never throws; every degradation lands in the ordered notes[] contract. (2) SENTINEL AS A SERVICE: every terminal "Sentinel / LA" label became "Log Analytics workspace"; the new sentinel-service pattern (requires sentinel) stacks "Microsoft Sentinel" -> "Sentinel content" ON the workspace node, so selecting only log-analytics draws no Sentinel (pinned both ways). The "(reduced)"/"(findings)" label variants collapsed into the workspace label - the subset story lives on the edge labels. (3) PER-SERVICE PRIVATE ENDPOINTS: the blanket private-link resource became private-link-law/-blob/-eventhub/-adx; private-ingestion requires private-link-law and gained the DNS: privatelink.monitor.azure.com requirement node; three new patterns (private-blob/eventhub/adx-endpoint) each draw the service's privatelink DNS zone node ("resolves to the private IP") plus the vNet-path edge, with disable-public-access considerations.

2026-07-29 status, architecture page full build-out (approved plan, seven slices): (1) BUG FIX - Winlogbeat and Splunk UF became transport NODES (the unify merge dedupes edges by node pair, so three direct endpoints->Stream methods collapsed to one line; WEF remains the one true direct edge - pinned). (2) DiagramEdge gains cost?: "premium" | "economical" under a pinned target-class rule (analytics-tier targets premium, low-cost store/egress targets economical, transit neutral) applied across all 20 patterns and propagated through unify. (3) ARCHITECTURE_PRESETS (6 one-click journey selections, each pinned to >= 1 match) + PRODUCT_WHEN_TO_USE (Record over the product union, compile-time exhaustive; "what it is" reuses catalog descriptions). (4) SOLUTIONS-DRIVEN SOURCES: sentinel-content/ingestion-classification exports SOLUTION_INGESTION_ENTRIES (the shipped ~436-solution asset as a sorted list - the OFFLINE enumeration surface); new architecture-patterns/solution-ingress.ts maps kind+tier to an ingress archetype TIER-FIRST (shipped kinds include junk strings; push-logs-ingestion / pull-native with the dual-path honesty drawing / agent-legacy with the _CL+alias alternative), builds dynamic patterns (id solution:<slug>, source label "<Name> (solution)" - collision-proof under canonicalNodeKey, resolved by a generic diagramNodeInfo fallback), and recommendWithSolutions expands selection.solutionSources (unknown names fall back to agent-legacy); recommendPatterns gains an optional extraPatterns parameter (backward compatible).

2026-07-29 status, live-dataflow detail + honest Sentinel/DCR corrections (user directives): (1) COLLECTOR JOBS - scheduled collectors are JOBS, not /system/inputs entries: the snapshot gains a jobs section (fetchLiveArchitecture now issues SEVEN GETs, /jobs added), normalizeInput reads the breakerRulesets/pipeline that jobs nest under their input. sub-object, and every input carries aliases (jobs answer to collection:{id}) resolved through an inputByAlias map so route filters attribute correctly; parseRouteFilterInputs gains the __inputId.startsWith('...') prefix form (matched against aliases). (2) STAGE BADGES - every live node carries a badge naming its stage/type ("{type} source", "Event breaker", "Pre-processing", "Routing table", "Pack", "Pipeline", "Post-processing", "{type} destination", "Cribl Lake"); DiagramNode gains optional badge and overlays fields. (3) SENTINEL AS A TAG, not a node (user rejected the chained sentinel-service pattern): the pattern is REMOVED; applySentinelOverlay(diagram, selection) tags the "Log Analytics workspace" node with overlays: ["Microsoft Sentinel"] when the expanded selection includes sentinel - idempotent, pinned both ways (log-analytics alone tags nothing). (4) DCR HONESTY - blob-archive-replay and lake-tiering routed their hot subset Stream -> workspace DIRECTLY (user report: no DCR); both now route stream -> Kind:Direct DCR ("hot subset") -> workspace (premium), pinned by the negative edge assertion. (5) DNS LOOKUP EDGE - all four private-endpoint patterns gained "worker DNS lookup" (Cribl Stream (vNet) -> DNS zone node) so the resolution chain reads worker -> DNS -> "resolves to the private IP" -> endpoint (user report: the DNS object floated unconnected).

2026-07-29 status, live-dataflow navigation + endpoint focus (user directives): (1) RESOURCE LINKS - BuildLiveDiagramOptions.uiBase (the leader UI base) makes every live node's info popover link to ITS page in the Cribl UI instead of the generic docs (withResourceLink REPLACES docs, pinned): routes hub -> /m/{g}/data-routes, sources -> /data/sources, collector jobs -> /jobs/collectors (the one slug pinned verbatim from docs.cribl.io/stream/common-errors), destinations -> /data/destinations, breakers -> /knowledge/breakers, pipelines/packs -> item-specific /pipelines/{id} and /packs/{id}; all slugs live in the ONE UI_PAGES map so a drifted slug is a one-line fix. criblUiBaseFromLeaderUrl derives the base from a configured leader URL (Cribl.Cloud hosts get the /stream product prefix; on-prem serves the UI at the root; blank/malformed -> undefined -> doc links stay). (2) ENDPOINT FOCUS - focusSources/focusOutputs options keep only flows FROM the listed input ids / INTO the listed output references (configured output id, or the raw route reference for synthesized "cribl_lake:{ds}"/"default" nodes); each kept flow keeps everything in-between; composes with azureOnly (focus applies first, both note honestly); empty intersection = empty diagram + "Focus: showing 0 of N" note.

2026-07-29 fix, UI_PAGES slugs: the menu-name guesses 404ed in the Cribl.Cloud SPA (user report). The real slugs were extracted VERBATIM from the workspace UI bundle route registry (nav enum Inputs="inputs"/Outputs="outputs"/Routes="routes"/Packs="p"; collectors at inputs/collectors; Knowledge breaker tab "breakerrules"; item templates pipelines/{id} and p/{packId} confirmed in-bundle). Method for next drift: the workspace serves the SPA shell for every path (server always 200 - the 404 is client-rendered), so probe by downloading main.js + webpack chunks and grepping the route/nav registries.

2026-07-29 status, Search send path in its own color (user directive): Cribl Search never writes the workspace directly - the send operator forwards findings to a Cribl HTTP source on a Stream worker group and Stream delivers them through the DCR (docs.cribl.io/search/send, pinned). All three Search patterns (search-in-place, search-sentinel-data-lake, adx-search-in-place) replaced their direct search->workspace "findings only" edge with search -> Cribl Stream ("send findings (Cribl HTTP source)") -> Kind:Direct DCR -> workspace (premium). DiagramEdge gains tone?: EdgeFlowTone ("search") - the send path's distinct line color - propagated through unifyPatternDiagrams like cost; in merged views the shared stream->dcr->law legs keep their first-pattern identity (they genuinely carry both flows) while the search->stream return leg stays violet end-to-end.

2026-07-30 status, routes detail + pack internals (user directives): (1) ROUTES POPOVER - routesHubInfo now renders one line per route (routeLine): name, (copy) marker, the FILTER verbatim (missing = "true (all events)"), "via pack X" / "via pipeline Y", and the destination with the default indirection resolved ("default (sentinel_dest)"); newline-separated (the popover CSS honors pre-line), capped at 10 + "+N more". (2) PACK INTERNALS - all-inclusive packs carry their OWN sources/routes/pipelines/destinations invisible to group-level sections: the snapshot gains packDetails (per-pack GET /p/{pack}/system/inputs|system/outputs|routes|pipelines, fetched by the usecase for up to MAX_PACK_DETAIL_FETCHES=8 installed packs via installedPackIds; the builder notes the shortfall). Embedded endpoints draw THROUGH the pack card (in:{pack}/{id} -> pack:{id} -> out:{pack}/{id}, typed badges, cost tiers, pack-page resource links); the pack popover appends embedded source/destination lists, the pipeline count, and the pack's internal route lines. Azure filter treats the pack as the flow unit (skips whole with a note); focus filters address embedded endpoints as "{pack}/{id}"; pack flows suppress the misleading "no flows" note.

2026-07-30 status, flow inventory + pack explode (user directives): (1) FLOW INVENTORY - "inventory the complete flows and let the user select the visual rendering": LiveDiagramResult gains flows[] (LiveFlowSummary: stable key "g:{input}>{routeId}>{output}" / "p:{packId}", human label "source -> route (pack/pipeline) -> destination", azure flag), computed BEFORE every filter so the list is complete regardless of toggles; BuildLiveDiagramOptions.selectedFlows draws only the chosen keys (composes focus -> selection -> azure, honest "Flow selection: X of Y" note; a selection without the pack key skips that pack's internals). (2) PACK CARDS labeled by displayName (user report: "ms-sentinel" should read "Zscaler Internet Sentinel"); DiagramNode gains expandable/expanded and BuildLiveDiagramOptions.expandedPacks EXPLODES a pack card into its internal chain: pack -> routes:{pack} hub (internal routeLine detail in the popover) -> pipe:{pack}/{id} ("Pack pipeline") -> out:{pack}/{id} with route-name edge labels and cost tiers; internally-unreferenced destinations keep the pack -> out shortcut; collapsed packs draw as before. PackInternals now carries the full pipelines list + displayName + azure.

2026-07-30 status, routing-table explode + disabled routes (user directives): BuildLiveDiagramOptions.expandRoutes explodes the group Routes hub the same way packs explode - each ENABLED kept route draws as its own node (route:{id}, label = name (+" (copy)"), badge "Route", popover = "Route entry" routeLine with the REAL table position + routes-page link) between the hub and its pack/pipeline/destination, and downstream edges drop the route-name labels the node now carries; each DISABLED route draws as a SUBDUED node off the hub (DiagramNode.muted / DiagramEdge.muted - new render-model flags; label "{name} (disabled)", badge "Route (disabled)", popover says it processes nothing) WITHOUT pulling its destination chain, and only when no flow selection is active. The hub carries expandable/expanded; collapsed behavior is unchanged (pinned). Route-referenced pack nodes in the group egress are now ALSO labeled by displayName.

2026-07-30 fix, pack-internal routing table explode: the "Pack routes" hub inside an exploded pack had no explode button (user report). BuildLiveDiagramOptions.expandedPackRoutes (pack ids; meaningful only alongside expandedPacks) gives that hub the same expandable/expanded contract: each enabled internal route becomes route:{pack}/{routeId} (badge "Route", popover = routeLine with the pack-table position + "(inside pack ...)"), downstream edges drop the labels the node carries, and DISABLED internal routes draw subdued off the hub without destination chains - mirroring the group-level explode exactly.

2026-07-30 fix, clean route + pipeline popovers (user reports): (1) exploded ROUTE NODE popovers no longer dump the raw filter expression - routeEntryPurpose renders "Route '{name}' - entry N of {scope}[, non-final copy]." then "Pipeline: {id | pack X | passthru (no processing)}" and "Destination: {ref}" as labeled lines (default resolution shown; DISABLED variant keeps its notice); the filter detail stays one level up in the routing-table hub popover, group and pack variants share the helper (pinned exact). (2) PIPELINE popovers list every function BY TYPE numbered in evaluation order (disabled ones marked, capped at 15 + "+N more") - LivePipeline.functionIds (first three) became functions: LivePipelineFunction[] {id, disabled}.

2026-07-30 status, structured popovers everywhere (user directives): DiagramNodeInfo gains three structured surfaces the renderers draw VISUALLY - facts (labeled rows), steps (numbered pills), routes (route-table bubbles: name + pipeline/destination chips, filter as hover tooltip, disabled dimmed) - replacing every prose dump in the live builder. Routing-table hubs (group + pack) and pack cards carry routes rows (the group hub now lists ALL routes including disabled, mirroring the real table; caps at 12 with an honest "Showing the first N"); route entry nodes carry Pipeline/Destination facts (routeEntryParts - the routeLine/routeEntryPurpose text formats are gone); pipelines carry steps (functions by type, in order, disabled marked, capped 15); sources carry Type/Collector/Schedule/Container/Event breakers/Pre-processing facts; destinations Type/Post-processing; breakers Rules; packs Pack/Version plus embedded-endpoint and pipeline-count facts. All pinned exact.

2026-07-30 fix, disabled routes keep their chain (user follow-up): exploded disabled routes no longer dangle - the group and pack variants both link the route's pipeline (pack:X -> the pack card, plain ref -> pipe node) and its resolved destination (real output, or the synthesized cribl_lake node) with MUTED edges carrying no cost badges; nodes already lit by enabled flows keep their normal look (addNode first-wins) while nodes existing ONLY for a disabled route draw muted - pinned both ways (shared splunk_dest stays unmuted, disabled-only quarantine_pipe draws muted).

2026-07-30 status, reference-pattern realism audit (user directive "closer to real world"): the audit confirmed every Cribl->workspace path already rides a DCR with cost tiers and the ingress/private/search patterns carry real transports (earlier rounds); the flagship gap was the MIGRATION story. New splunk-siem-migration pattern (requires stream+sentinel+windows-splunk-uf; the siem-migration-splunk preset now matches it): ONE diagram tells before/during/after - the BEFORE edge (UF -> Splunk indexers direct) draws MUTED, the AFTER edge re-points the same UF fleet at Stream (S2S), Stream dual-writes "during: dual-run until cutover" to the legacy indexers AND through the Kind:Direct DCR into the workspace (premium); considerations spell out the five phases (intercept passthrough / dual-run / content conversion via the SIEM Migration screen / parity validation / cutover+retire). New "Splunk indexers (legacy)" info entry (Splunk LB destination doc). ENABLER: unifyPatternDiagrams now propagates muted on nodes AND edges (it silently dropped them - reference patterns could never draw subdued before/after states).

2026-07-30 fix, HF tier in the Splunk migration (user correction): real estates funnel UFs through Heavy Forwarders, and the HF tier is the migration's FASTEST intercept - splunk-siem-migration gains the "Splunk Heavy Forwarders" node: the muted BEFORE chain is now uf -> hf -> indexers, the bright TEMPORARY bridge hf -> stream ("during: HF outputs to Stream (temporary)") carries the estate until UFs re-point directly ("after"), and Phase 1/Phase 5 considerations cover intercept-at-the-HF-tier and retiring the HFs (Stream absorbs parsing/routing; keep an HF only while it hosts un-re-homed modular inputs). Info entry for the HF label added; unify pin moved to the hf -> indexers muted edge.

2026-07-30 fix, Splunk edges premium + vendor-neutral wording (user directives): (1) edges INTO the Splunk indexers carry cost "premium" - per-GB license-billed ingest, both the muted before leg and the bright dual-run leg (Phase 4 now says dual-run pays BOTH premium bills and to time-box it); the EdgeCostTier rule, the cost-rule test's PREMIUM_TARGETS, and the legend/badge wording ("per-GB ingest billing - Sentinel analytics tier or SIEM license") widened beyond Azure. (2) VENDOR NEUTRALITY (legal/marketing directive: no bias, fair to each vendor): the "(legacy)" suffix is GONE - the node is "Splunk indexers"; "retire (and the license)" became "decommission the ... tier"; "Splunk retires" became "the previous SIEM is decommissioned"; all wording states the customer's migration factually, never a judgment about a vendor.
