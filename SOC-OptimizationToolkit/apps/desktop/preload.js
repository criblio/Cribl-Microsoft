// Phase 1 wires the context bridge here: contextBridge.exposeInMainWorld('api', { ... }),
// the single typed surface the thin renderer talks to (it never imports adapters or core directly).
// Empty stub for the Phase 0 shell.
