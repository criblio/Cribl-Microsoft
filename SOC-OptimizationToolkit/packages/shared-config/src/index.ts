// @soc/shared-config — the declarative param-forms schema and integration-mode gating.
// Pure data + validation, consumed by core and rendered by the frontends. Shell for now.
export const PARAM_FORMS_VERSION = 0 as const;
